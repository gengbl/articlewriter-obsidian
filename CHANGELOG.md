# 变更日志

每次功能更新 / bug 修复完成后按「AGENTS.md · 工作更新写回约定」追加一行：日期 + 一句话摘要（一次任务的多个改动可合并成一条）。新条目追加在文件末尾。

- 2026-08-30：状态面板右键章节目录「新建文章…」路径修复（改用 `listChapters` 返回的真实章节目录，不再用会重复编号的 `chapterDirPath(story,num)`）；状态面板拆为固定头部 `.aw-st-top` + 可滚动树主体 `.aw-st-tree`（仅全局文档/章节树内部滚动，错误提示挂滚动区）；main.ts 订阅 vault 文件变更事件（workDir 内 .md）→ 防抖 800ms 自动刷新已打开状态面板，章节字数随写作落盘实时跟进，render 保持树区滚动位置；以上改动打包发布 **v0.0.3**（`articlewriter-v0.0.3.zip`）。
- 2026-08-30：状态页三处展示修复合并归档——①字体对齐侧栏文件列表（`.aw-status-view` 改引 `--nav-item-size`(13px)/`--nav-item-color` 专用变量、移除遗留 em 字号规则）；②更新时间改本地时间显示（存储恒 UTC ISO，新增 `story_types.formatLocalDateTime(iso,full?)`，面板与 /status 共用）；③「小说状态」书名行改版（更新时间移至书名同行右对齐 `.aw-st-status-title` flex space-between、`YYYY-MM-DD HH:mm` 含年份，第三行仅章数·当前章·总字数）。
- 2026-08-30：打包纳入写作指南——esbuild.config.mjs 把根目录 `WRITING_GUIDE.md` 一并拷入 `release/`，发布 zip 与部署均改为四个文件；AGENTS.md「运行环境与构建部署」新增打包条目。
- 2026-08-30：新增 RELEASE.md（代码提交 / 版本打包 / Gitea Release 发布的完整流程脚本 + 实测坑位），AGENTS.md Git 节增加引用；修正 v0.0.4 Gitea 附件漏 WRITING_GUIDE.md（重打 release/ 全量包并重建 Release）。
- 2026-08-30：v0.0.5 插件目录审核修复——banned_words.ts/prompts.ts 移除全部 lookbehind 断言（iOS Safari <16.4 会抛 SyntaxError；改「(?:^|[^X])」前缀组 + $N 回补模式，2万条随机语料对照旧规则零差异验证），正则统一编号捕获组；modals/llm_chat_view/main.ts 的 JS 内联样式赋值迁入 styles.css 静态类（`.aw-action-list`、`.aw-prompt-textarea`、`.aw-stream-*`、`.aw-chat-status.is-hidden/is-error` 等）；getEmptyStateElement 不再用 document.createElement 返回游离节点；revealLeaf→setActiveLeaf；TFolder 断言改 instanceof 收窄；删除未使用变量/常量（reason、CHAPTER_INFO_KEYS）、catch(_e) 空块；manifest 描述加英文+author=fosky，新增 MIT LICENSE，README 增加英文概览节。
- 2026-08-30：Lint 清理与设置页现代化——llm_client.buildParams 去 `as any`（改返回带索引签名的 `LlmChatBody`，model 可空+透传 openai_extras）；modals.ts 事件回调/选择器覆写统一 `void` 包裹消除「Promise returned where void expected」；删除操作 ×4 处由 `vault.trash(file,false)` 迁移到 `app.fileManager.trashFile(file)`（跟随用户删除方式设置）；移除 7 处冗余 `createEl(...) as HTMLElement*` 断言（本地 patch dts 的 createEl 本身泛型返回精确类型）；设置页新增声明式实现 `getSettingDefinitions()`+`getControlValue/setControlValue`（Obsidian ≥1.13 生效：LLM 配置渲染为 list+子页支持新建/删除/重排，key 约定 cfg.<下标>.<field>），旧 `display()/renderLlm()` 保留作 <1.13 回落。打包发布 **v0.0.6**。
- 2026-08-30：v0.0.7——manifest minAppVersion 由 1.4.0→**1.13.0**（声明式设置 API @since 1.13.0、fileManager.trashFile @since 1.6.6；社区插件目录校验 obsidianmd/no-unsupported-api 要求声明版本覆盖全部所用 API）；RELEASE.md 增补 GitHub 镜像 Release 资产要求（main.js/manifest.json/styles.css 必须作为独立附件上传，zip 可并存仅作额外资产）。
- 2026-08-30：README 语言布局调整——英文版升为默认 `README.md`（GitHub 页面默认展示英文），原中文版改名 `README_ZN.md`（原 README_EN.md 即升入 README.md，不再单独存在），两份交叉互链。
- 2026-08-30：新增 GitHub Actions 发布工作流 `.github/workflows/release.yml`——推送新标签（或手动 Run workflow）自动 npm ci+构建+打 zip，经 actions/attest-build-provenance 对 main.js/styles.css/manifest.json 做来源签名后创建/更新 Release（消除目录校验的 attestation 建议项；tag 与 manifest version 不一致即失败）。
- 2026-08-30：Lint 告警清零——根因是 tsconfig lib≤ES2018 但 `@types/node` 静默拉入 es2020 String 扩展致外部 lint 将 `matchAll`/`trimStart` 判为 error-typed（报 no-unsafe-member-access ×27）：banned_words.ts 标题行判断改 `/^\s*#/.test()`、md_docs.ts extractForeshadows 与 llm_chat_view.ts resolveRefs 两处 matchAll 循环改 exec while 循环（静态 /g 正则扫描前 lastIndex=0 复位）、prompts.ts 大纲要点提取改函数内局部 /gm 正则 exec 循环；main.ts 两处设置页下拉框 `Object.fromEntries(name→name)` 恒等映射改用 `reduce` 构造恒等 Record（本地 patch dts 的 addOptions/声明式 dropdown 只接受 Record<string,string>，不能改传字符串数组）；新旧实现经 CRLF/孤立 CR/lastIndex 残留回归语料等价性对照零差异。另将中文 UI 文案中全部全角空格 U+3000（main/modals/status_view 共 30 处，no-irregular-whitespace ×16）替换为半角空格。AGENTS.md「运行环境与构建部署」新增 API 版本上限与全角空格两条陷阱约定。
- 2026-08-30：v0.0.8 校验器二轮告警清零——删除 main.ts 已废弃 `display()/renderLlm()` 旧渲染路径及 llmSelName 状态、孤儿 Setting import（minAppVersion=1.13 保证恒走声明式，校验器禁 deprecated API）；PLAIN_REF_RE 字符类去多余转义改 `[^\s:@[\]]`；safeFilename 控制字符正则改模块级 RegExp 动态构造（no-control-regex，字符集严格对齐 Python `\x00-\x1f` 不含 DEL/C1）；story_manager renumText replace 回调捕获组显式标注 `(d:string)`（no-unsafe-argument）；main.ts 移除 6 处冗余类型断言/非空 `!`、pickWorkDir action 加 void 包裹（promise→void）；esbuild 增出 dist/main.js + .gitignore /dist/（校验器在根/dist/build/out 找构建产物）；builtin-modules 依赖改用 Node 内置 node:module builtinModules。新旧正则经码点逐一对比+模糊语料等价性验证零差异。CI release 工作流调整为先发布 Release 再 attest（attest persist/create-storage-record 在对应 Release 尚不存在时报 Failed to persist attestation）。
- 2026-08-30：v0.0.9——仅 CI 修复，插件代码无变化。release.yml permissions 补 `attestations: write`：actions/attest-build-provenance 的 persist（create-storage-record）经 REST create-an-attestation 落库需要该仓库级权限，缺失时报「Failed to persist attestation: Resource not accessible by integration」（Rekor/Fulcio 透明日志条目不受影响、照常生成）。注意 tag 触发的运行取打 tag 时的旧 workflow，验证须走新 tag 或 workflow_dispatch。
- 2026-08-30：AGENTS.md 按功能域拆分为 `agents/` 下 8 个子文件（scope/build-deploy/code-structure/data-model/command-reference/ui-conventions/workdir-behavior/process-rules），原 AGENTS.md 改为纯路由索引；写回约定目标由「本文件」改指对应子文件，内部交叉引用同步更新。
- 2026-08-30：修复 story_types.ts FN_BAD_CHARS_RE 的 no-control-regex 误报升级问题——新版校验器会解析 new RegExp 字符串内 \uXXXX 转义，C0 区间改为 String.fromCharCode 运行时拼接入字符类（源码零控制字符记号），106 用例新旧行为等价验证通过；build-deploy.md 坑位条目同步更新。
- 2026-08-30：YAML 依赖由 js-yaml 换为 `yaml`（eemeli/yaml，自带 TS 类型、无安全通告）以过社区目录校验器依赖检查；state_doc.ts 改 parse/stringify（lineWidth: Infinity 不折行），库级+函数级往返测试全通过（含旧格式文件兼容读取、extra 透传、正文保留）。
- 2026-08-30：README.md 顶部新增英文 Disclosures & Security Statement（社区审核披露）——网络请求仅为用户自配端点的 LLM chat completions、无遥测/作者服务器；atob/btoa 全部来自打包的 yaml/openai SDK 标准数据处理，插件源码零 base64 调用。
- 2026-08-30：以上三项（no-control-regex 控制字符修复 / js-yaml→yaml 换库 / README 中英审核披露声明）随 manifest bump 打包发布 **v0.0.10**（`articlewriter-v0.0.10.zip`）。
- 2026-08-30：发布约定变更——自 v0.0.10 起不再在 Gitea 创建 Release / 走 Web 会话上传流（社区目录审核只认 GitHub Release）；发布 = Gitea tag vX.Y.Z + 镜像无 v 前缀 tag 触发 CI 自动上架。RELEASE.md 第 5–7 步标停用，AGENTS.md 路由行与 build-deploy 同步修订。
- 2026-08-30：发布约定再澄清——Gitea 侧不建/不拉起任何自动化工作流（Release 仍按 RELEASE.md 5–7 步手动维护）；GitHub CI 不再打包/上传 zip，release.yml「Package zip」步骤改为仅读取 manifest version、Release 资产只含 main.js/manifest.json/styles.css 三散文件（zip 附件只在 Gitea Release）。
- 2026-08-30：README 审核披露声明追加「§3 Vault Enumeration Disclosure」（中英双版）——如实说明两类 vault 遍历：全库级仅路径列举两处（work_dir 文件夹选择器 collectFolders 从根递归但限深 3 层/上限 500 条/跳过隐藏目录；聊天面板 @引用候选 getFiles()），其余功能全部限定用户自选 work_dir 内，内容仅在用户实际操作后本地读取、绝不外传。同步修正 AGENTS.md 路由行残留的旧发布约定措辞（Gitea Release 手动维护不建工作流）。
- 2026-08-30：README.md 披露声明 §3 末尾追加「Summary for reviewers」一句（供社区审核对话直接引用）——全库列举仅两个 UI 选择器且只暴露路径，其余文件访问限定用户自选工作目录、全程本地内存处理。
- 2026-08-30：纯文档/披露发布 **v0.0.11**（插件代码无变化）——README 审核声明追加 §3 Vault Enumeration Disclosure（中英双版，含 Summary for reviewers）、GitHub CI 不再打包/上传 zip、Gitea Release 手动维护不建自动化工作流的约定落进 RELEASE.md/build-deploy/AGENTS。
- 2026-09-02：主窗口改名后状态面板不同步修复——根因 main.ts 只订阅 vault `modify/create/delete` 未订阅 `rename`，文件列表右键改名/拖拽移动 .md 不会触发已打开状态面板刷新；补 `rename` 订阅（触发时 file.path 已是新路径，须同时看 oldPath 判断是否涉及 workDir[覆盖跨边界移入/移出]），复用同一防抖 800ms refresh 链路，tsc+esbuild 构建通过并部署至 geng_bl vault。
- 2026-09-02：以上修复随 **v0.0.12** 打包发布（Gitea tag v0.0.12 + GitHub 镜像无 v 前缀 tag 触发 CI）；GitHub Release 资产由三个散文件扩为四个——release.yml 新增上传写作指南 `WRITING_GUIDE.md`（与 Gitea zip 内容对齐、attestation 一并覆盖）；AGENTS.md 新增「推送指南」节（改动同步到 articlewriter-obsidian-git 克隆目录后在该目录内提交推送），RELEASE.md/build-deploy 同步修订。
- 2026-09-02：发布约定澄清 + v0.0.12 收尾——GitHub 镜像同步至 v0.0.12（main + 无 v 前缀 tag `0.0.12`）触发 CI 自动上架，Release 资产为含写作指南 `WRITING_GUIDE.md` 的四散文件。**澄清：Gitea 侧实际存在 tag 触发的 `release.yml` Actions 工作流，会自动创建带 `articlewriter-v<版本>.zip` 的 Gitea Release（依赖实例有可用 Actions runner，缺则运行卡 `queued`）**；此前「v0.0.10 起停维护 / 不建自动化」的表述与实际不符（Gitea Release 已持续维护至 **v0.0.11**），AGENTS.md / build-deploy.md 相应更正并新增规则：发布后用 REST API `GET …/releases/tags/v<版本>` 核对附件字节数 == 本地同名 zip，缺失时起一台 runner 或用删远端 tag→重推的方式重新入队让其执行后再复核。
- 2026-09-02：移除 `.github/workflows/gitea-release.yml`（commit f255304）——放弃自托管 Gitea 的自动建 Release，Gitea 侧恢复为「无任何 Actions 工作流、Release + zip 附件按 RELEASE.md §step5 Web 会话表单流手动维护」（回到 v0.0.10 前约定，并更正上一条关于 tag 触发自动创建的表述）。同步清理：删除服务器凭据目录 `~/.config/articlewriter-gitea-release/`；AGENTS.md / build-deploy.md 相应改回手动口径。GitHub 发布不受影响（仍由镜像无 v 前缀 tag 触发 release.yml 自动上架四散文件）。注：实例上的 `gitea-runner.service` systemd 服务仍在运行但已无工作流可触发（彻底停用需 sudo disable --now）。
- 2026-09-02：**创作规范三层重构**——①系统级由「data.json 内嵌 + 可选 system_guide_path」改为**固定插件数据目录文件** `.obsidian/plugins/<id>/WRITING_GUIDE.md`（首启一次性播种、读不到回落内置默认；走 vault.adapter 裸读写，因该目录不被 metadata cache 索引），移除设置页 `llm.system_guide_path` 行及其 get/set 分支（字段保留为遗留种子）；②新增每书聚合文件 `<书名>/写作指南汇总.md` = 三层按类目合并落盘（serializeAggregateGuide + agg-hash md5 变更检测头），成为 LLM 写作命令与对话注入【创作规范】的**唯一来源**（stripComments 去注释正文），仅当三层任一 md5 变化才重算；③两个新命令 `generate-writing-guide`（对用户级+当前书各建同格式空模板 buildEmptyGuideTemplate，非空跳过并提示）、`regenerate-system-guide`（confirmBox 二次确认后以内置默认覆盖系统级文件并刷新当前书汇总）。prompts.ts 增补 buildEmptyGuideTemplate/serializeAggregateGuide/AGG_TITLE/AggHashes/embed·parseAggHash；story_manager.ts 增 pluginFileExists/readPluginFile/writePluginFile。tsc 零错误 + esbuild 通过 + node --check OK + 真实源码 bundle 冒烟 13/13。
- 2026-09-02：以上「创作规范三层重构」随 **v0.0.13** 打包发布（manifest 0.0.12→0.0.13；Gitea tag `v0.0.13` + GitHub 镜像无 v 前缀 tag `0.0.13` 触发 CI）——系统级写作指南固定为插件数据目录文件 `.obsidian/plugins/<id>/WRITING_GUIDE.md`（首启播种+回落内置默认）、每书聚合文件 `<书名>/写作指南汇总.md`（agg-hash md5 变更检测）成为 LLM【创作规范】唯一注入源、新增命令 generate-writing-guide / regenerate-system-guide；GitHub Release 资产仍为含 WRITING_GUIDE.md 的四散文件。
- 2026-09-03：新增「生成使用说明」——内置《使用说明.md》文本（src/usage_guide_default.ts：快速上手/命令一览/常驻面板与@引用语法/设置项/目录结构/标记速查），**设置或切换工作目录后自动投放到 work_dir 根**（seedUsageDoc 仅缺失或为空才写、不覆盖用户内容、失败不阻断切换），并加 generate-usage-doc 命令手动重建（非空跳过提示、成功打开该文件）；README×2/command-reference/code-structure/workdir-behavior 同步。
- 2026-09-03：状态面板树节点开合提速（手机尤甚）——①StatusView 加 lastSnap 缓存 + rerenderLocal()：章节行/箭头与分组标题的开合改为纯本地 DOM 重渲染，不再每次触发 getStatusSnapshot 全库读盘（refresh 失败清缓存回落完整刷新）；②getStatusSnapshot 瘦身为只对当前激活书做 countWords+buildStoryDetail（视图本就只渲染激活书小节），非激活书仅 loadState 兜底，写作防抖自动刷新的 IO 同步下降。
- 2026-09-03：常驻面板更名「工作状态」→「写字台」（getDisplayText、命令名、ribbon 提示、打开失败通知及 README×2/使用说明内置文本/agents 文档同步）。
- 2026-09-03：插件启动自检《使用说明.md》（ensureUsageDocOnStartup）：onload 时 workDir 已设且文档缺失/为空 → 自动创建并在 workspace 布局就绪后打开（openWhenLayoutReady；真·首启无工作目录则等 pickWorkDir 投放，其新建时也自动打开）。
- 2026-09-03：故事状态.md chapters 元数据瘦身——ChapterMeta 移除无消费方的 tags/note 字段（解析忽略旧值、序列化不再写出、扫描重建不再镜像），标签/备注仍以各章《章节信息.md》为唯一来源；data-model.md 同步。
- 2026-09-03：新增「插入章节」命令 insert-chapter——在当前激活章（无则选参照章）之前/之后建空章，≥插入位的各章整体+1（两阶段临时迁移），同步重写各章文档第N章/章节:N引用与伏笔.md；断档空位直插不挪动他人；完成后聚焦新章（manager.insertChapter）。
- 2026-09-03：写字台章节行右键菜单新增「在本章前/后插入章节…」（StatusAction kind=insert-chapter → manager.insertChapter），与命令面板入口同语义。
- 2026-09-03：删除中间章节后自动补洞——deleteChapter 在被删号之后仍有章节时复用 renumberChapters 将后续各章整体 -1（两阶段迁移+文档/伏笔引用重写），保持 1..N 连续；两处删除入口（命令面板/写字台右键）确认框与完成提示同步说明。
- 2026-09-03：《使用说明》独立文档化——内容移至 docs/使用说明.md（含插入章节、删除自动重排号等新增说明），esbuild .md=text 内联进包，生成时从代码恢复写盘；AGENTS.md 增加「每次修改必须同步 docs/使用说明.md」硬规则。
- 2026-09-03：三层写作指南模板全部独立文档化打包——系统级默认内容直接内联仓库根 WRITING_GUIDE.md（与内置常量逐字节一致），用户/小说级空模板固化为 docs/WRITING_GUIDE_template.md；esbuild .md=text 打包进 release/main.js，运行时从包内恢复写盘；AGENTS.md 硬规则扩展为「功能修改须同步对应内置文档」。
- 2026-09-03：修复删除/插入触发重排时产生重复大号章节的 bug——根因是 vault.rename/trashFile resolve 后文件树可能尚未更新，后续扫描把已删/已移走的目录再纳入两阶段迁移。story_manager 新增 settleTree（轮询等待树状态）+ assertUniqueNums（重复号残留即拒绝）；moveChapterDir 每步改名后强制校验生效否则中止；renumberChapters 只移动真正变号的章；deleteChapter 回收站后先确认目录已从树中消失。
- 2026-09-03：清理历史重排 bug 遗留的空心章节目录（仅含 .space/context.mdb、无任何正文，被自动重排压缩进连续区间后显示为 0 字幽灵章节）——八仙剧本 #24/#75、灵境神念 #07-11、都市传说 #14/#15，共 9 个，全部移入各书 _backup/幽灵目录清理_<时间戳>/ 可恢复；涉及的状态文档脱节用「扫描重建小说状态文档」刷新。
- 2026-09-03：幽灵章节 bug 第二轮根治——定位到外部插件 make-md 会在任何新建目录（含两阶段迁移的临时槽位）里写 .space/*.mdb，旧代码索引校验滞后中断后留下的临时槽即成「空心幽灵章」。story_manager：listChapters 一律忽略无「章节.md」的空心目录与元数据索引陈旧条目；settleTree 改用 adapter.exists 真实文件系统轮询校验；新增 resolveChapterDir（按号定位带原始 FS 兜底）、quarantineHollowChapters（直接扫原始 FS、把空心目录隔离进 <书>/_backup/空心残留_<时间戳>/）并在 insert/delete/renumber 操作前预隔离+成功后收尾清扫；moveChapterDir 改走 adapter.rename 整树改名。main.ts 加 BUILD_STAMP 加载通知用于确认进程跑的是新代码。同期清理都市传说 #15/#16/#19/#20 空心残骸与各书 .aw-base（Python 版同步遗留）。
- 2026-09-03：移除写字台「重建状态」按钮（用户反馈暂不需要），故事状态.md 的磁盘重建统一走命令面板「扫描重建小说状态文档」（/scan）；相关 UI/StatusAction/handleStatusAction 代码与文档描述已回退。
- 2026-09-03：移除启动时的「写字台已加载（构建 …）」验证通知（BUILD_STAMP），幽灵章节修复经用户确认生效，无需再靠它辨别代码代际。
- 2026-09-03：章节按卷实体目录——每卷在书根下拥有同名实体目录 <书名>/<卷名>/，归属该卷的章节目录物理存放其中；未归属章节留书根（属正常布局）；章号全书连续、跨卷不重置。卷名须唯一（作为目录名）：新建/改名重名即报错；改卷名=同步 rename 实体目录（失败回滚元数据）；删卷先把其下章节移回书根再清元数据并 rmdir。建章可带目标卷（createChapter 第4参，直接落卷目录+写双份归属字段），插入章自动继承参照章所在容器；rename/move/renumber 均保留父容器；listChapters 扫描「书根+各卷实体目录」（containerPaths，返回 vol?/parentPath），位置即归属——/scan 以位置为准就地回填不一致的《章节信息》「卷」字段并在结果中报告修正处数。新增命令「按卷整理目录 /organize-volumes」（幂等迁移：平面结构残留批量归位，报告移动/未归属/未识别容器）；切换书籍（/dir 或写字台选书）检测到平面残留强制自动执行且不可跳过，失败置 flatBlocked 锁定该书全部章节/卷结构操作直至手动整理成功；各结构命令入口另有 ensureVolumeLayout 门禁（确认框引导立即整理，取消=阻断本次操作）。顺带修复 CHAPTER_DIR_RE \d{1,3}→\d{1,6}（章号上限 999 的历史 bug）。
- 2026-09-03：「按卷整理目录」增量——书里一个卷都没有时先弹输入框引导连续建卷（留空/取消结束；新建与既有章节目录同名的卷→其中章节立即自动归属），再继续整理；整理收尾对仍留书根的无归属章节逐个 pickAction 选卷归位（仅一卷→一次 confirmBox 整体移入；选项含「留在书根」「停止分配」，Esc=结束本轮）。
- 2026-09-03：「管理卷→分配章节」由单章号输入升级为章节选择表达式（all/全部、区间如 3-7〔或三至七〕、列表如 1，4，5——复用 md_docs.parseChapterSelection 打包合集同款解析）；多于一章时二次确认列出清单（超 12 章截断显示+总数）、无法识别片段提示后忽略、单章失败不中断其余。
- 2026-09-03：切换书籍的强制按卷整理补零卷场景——原判定 needsVolumeOrganize 对零卷书恒返回 []、遗留子目录（如含章节的旧分组文件夹）永不触发；现扩展为两种残留均触发：有卷且已归属章留书根 / 零卷却存在未识别章节目录，且两条路径（手动命令与切换强制迁移）在迁移前统一先过 promptCreateVolumesIfEmpty：全书无卷时弹输入框引导连续建卷（新建与遗留目录同名的卷→其内章节立即自动归属）。
- 2026-09-03：写字台面板「章节」小节升级为卷树——归属了卷的章节挂在可折叠的「第N卷 · 卷名（X章）」节点下（点行/箭头开合，会话内记忆），未归属章节平铺兜底；各卷行尾带激活 Radio（选中=设 current_volume 并跳到该卷最后一章、打开正文，对齐 /volume use）；卷节点右键菜单：在本卷新建章节…（直接落该卷实体目录并设为当前卷）/ 新建卷… / 重命名本卷…（同步移动卷实体目录）/ 删除本卷（级联删其全部章节：确认框列明受影响章节后章节目录整体移入回收站、末尾单次补洞重排——manager.deleteVolumeCascade，区别于管理卷里 deleteVolume 的解绑回移语义）；书籍列表标题行与全局文档区右键新增「新建卷…」入口（建成即设当前卷，对齐 /volume add）。快照 StatusDetail 增 volumes/currentVolume、StatusChapterEntry 增 volumeId（按物理位置判定）。
- 2026-09-03：新增「导出卷合集」——某卷全部章节正文合一 MD（与 /pack 同款格式：中文章节号标题 + --- 分隔、空正文章跳过并在通知里列出）。manager.packVolume 按物理位置判归属（listChapters vol===卷id，故各入口先过 ensureVolumeLayout 平面残留门禁），默认文件名 <书名>-<卷名>-合集.md、输出路径可自定义（带扩展名视为完整文件名）；无章时报错提示先执行「按卷整理目录」。/pack 原实现拆出共用的 buildPackParts/writePackFile 私有装配（对外行为不变，含文件名取实际有正文的首末章边界）。三个入口：命令面板「导出卷合集」、管理卷菜单「导出此卷合集」（无章禁用）、写字台面板卷节点右键「导出本卷合集…」（StatusAction export-volume）。
- 2026-09-03：**v0.0.15 每卷独立编号（复合键身份模型）**——章节身份由全局连续章号改为复合键 `"volId:N"` / `"N"`（state_doc chKey/parseChKey）：`current_chapter` 改 string|null、chapters 键改复合键，旧格式加载时自动归一化重映射；createChapter 去掉章号参数（目标容器内 max+1 自动顺延）、insert/delete/renumber 补洞与引用改写全部限定在参照章所在容器（卷/书根）内进行，跨卷可同号不再冲突；提示词层改用阅读序位置 ordinal（loadWritingData chapterNum/localNum/volumeName + readOutlineWindow 前文大纲窗口 + OutlineRef prev/bridge 块），本地章号仅用于落盘标题与容器内引用；场景/人物记录 vol 字段消解裸章号歧义（命令输入裸章号经 resolveLocalNum 解析、多卷同号逐个确认）。全链路适配：story_manager/prompts/status_view/main.ts 各写作流（/write /continue /rewrite /review /polish /deai）与写字台快照。
- 2026-09-03：零卷书「切换书籍 / 按卷整理目录」的建卷引导由连环追问改为只弹一次——promptCreateVolumesIfEmpty 去掉 for(;;)，仅询问并创建单个卷（留空/取消 = 跳过直接继续），要建多卷请重复运行或用命令面板「新建卷」。
- 2026-09-03：主动新建卷改为批量建卷页——新增 VolumeBatchCreateModal（modals.ts）：顶部列已有卷名防重，输入框+「添加」逐行把新卷名加入待建列表（每行 ↑/↓ 调创建顺序、可删除），确定后由 main.ts createVolumesInOrder 按列表顺序依次 addVolume（单个重名失败不中断其余、末尾汇总 Notice），最后成功的卷设当前并可顺带建章归属。命令面板「新建卷」、写字台右键「新建卷…」、零卷书整理/切换引导三处统一走 pickNewVolumeNames；不再连环弹单卷输入框，也不再单独询问卷描述（建成后可在管理卷→编辑描述补填）。
- 2026-09-03：修复「卷内激活章节有时不生效」——根因是故事状态.md 的 chapters 映射与磁盘脱节（典型：卷实体目录被外部改名，章节目录变成匿名容器、面板行键退化为裸数字/旧复合键）时，写入 current_chapter 的键不存在于 chapters，下次解析被存在性校验静默丢弃。switchChapter/statusActivateChapter/activateVolume 写前统一经 ensureChapterEntry 按磁盘扫描回填缺失条目；current_volume 改以键内容器为准；activateVolume 末章号取状态文档与磁盘两源最大值。
- 2026-09-03：状态文档序列化放行「第0章」（序章）——formatStateDoc 原先跳过 num<=0 的条目，导致含第0章的书（如《最好的她》）激活该章后 current_chapter 在下次解析时因无对应条目被丢弃。
- 2026-09-04：**三层写作上下文重构 + 卷摘要自动提取**——①buildWritingContext 输出改书/卷/章三节（`== 故事情况 · 书籍级/卷级/章级 ==`）：角色/场景结构化列表按容器分层（本卷条目归卷层、其余归书层）并排除归属当前章的条目（其完整信息由章级 folderDocs 原文承载），人物关系拆书/卷/章三份清洗文本经 dedupeLinesAgainst 跨层逐行去重（优先级 章 > 卷 > 书，normalizeRelLine 归一化比较）、folderDocs 剔除《人物关系.md》；卷级节含本卷大纲/卷摘要/本卷人物关系/本卷角色设定/本卷场景。②新增插件特有「卷摘要」`<volDir>/卷摘要.md`（格式对齐章节摘要：H1+内容哈希头；哈希=本卷成员章 key|md5(正文) 拼接 md5，任一正文变化即过期）——五个写盘命令（/write /continue /rewrite /polish /deai）保存后 main.ts refreshVolumeSummary 尽力而为增量更新（最新章节 AI 摘要缺失则正文尾部 1500 字符节选 + 现有摘要 → LLM 合并 400~600 字滚动摘要），非阻塞、失败仅 Notice 警告；loadWritingData 仅在卷内且新鲜时注入【卷摘要】。③buildContinuePrompt 移除与写作上下文重复的全局/本章大纲全文注入（保留标记语义说明+无大纲回落提示）。prompts.ts 增 buildVolumeSummaryPrompt/VOL_SUMMARY_SYSTEM_PROMPT/dedupeLinesAgainst，story_manager.ts 增 computeVolumeHash/readFreshVolumeSummary/saveVolumeSummary/getChapterSummaryText，main.ts 五处 setChapterBody 埋点；data-model/scope/code-structure 文档同步。tsc 零错误 + esbuild 通过 + node --check OK，已部署 geng_bl vault。
- 2026-09-04：写字台面板卷行与章节行的激活 Radio 圆圈整体放大 40%（外环 10px→14px、内点 inset 1px→2px 同步等比，hover/checked 各态几何不变仅尺寸放大）；styles.css 部署至 geng_bl。
- 2026-09-04：修复「激活态 Radio 悬停时内圆与外圆不同心」——根因是 Obsidian app.css 对 `input[type="radio"]:checked::after` 叠了圆形 `-webkit-mask`（mask-size 65%/50%、position 52%/50%，见核心 :checked:after 规则），而自绘 `.aw-st-radio::after` 靠自身 background+border-radius 画内点却未清零该 mask；未选中态无此 mask 故同心，已选中态带 mask 再经 transform scale(2) 放大后内点被裁成偏移小圆与外环错位。`.aw-st-radio::after` 基类补 `mask-image/-webkit-mask-image:none` + `transform-origin:center`，让激活/非激活走同一条干净圆点路径；纯 styles.css 改动，npm run build 已同步 release/styles.css。
- 2026-09-04：**无卷模式（默认扁平结构）**——新增每本书的 `use_volumes` 开关（故事状态.md frontmatter），**新书 createStory 恒为 false = 纯「书籍→章节」扁平结构、不建卷/不归卷/不按卷整理**。向后兼容推断：旧文档缺该字段时 parseStateDoc 按磁盘事实回填（存在任一 vol 域复合键或 current_volume → true，否则 false），首次保存后恒显式写回使行为确定。**切换命令 `/volume off|on`**（main.ts cmdVolumeMode(enabled)，两命令 volume-mode-off/on）：off=破坏性拍平（若仍有卷则 flattenToRoot 逐章 relocateChapterContainer(key,null) 回书根连续重编号 + 各卷实体目录 trashFile 入回收站 + 清 卷.md 元数据与 current_volume，幂等可续跑、执行前二次确认）；on=仅置位不改盘。**门控面**：addVolume/setChapterVolume/organizeByVolumes 在 no-vol 下抛 NO_VOL_MODE_MSG；cmdOrganizeVolumes/cmdVolumeAdd/cmdVolumeManage/cmdPackVolume 入口 Notice 拦截（先于任何弹框）；enforceVolumeLayoutOnSwitch 对 no-vol 书直接放行跳过强制整理；createChapter 忽略 volId 落书根；写字台 StatusDetail.useVolumes=false 隐藏全部「新建卷」右键入口且空态文案不提建卷。tsc 零错误 + esbuild 通过 + node --check OK。
- 2026-09-04：通用动作列表 ActionMenuModal（切换书 / 插入位置 / 管理卷·场景·人物等一切"选一个实体再做操作"的菜单）交互优化——①鼠标悬停行文字加粗提示可点；②选中项前置 `▶` 标记并高亮底（`.aw-selected::before`，disabled 不显示），此前 `.aw-selected` 无任何样式、选中不可见；③**点行只移动选中标记、不再立即执行**，须再点「确认」CTA 或按回车才触发 onSelect（避免误触即改状态）。styles.css `.aw-action-row*` 补 hover/selected/disabled 规则，modals.ts renderList 点击回调去掉即时 choose()；ui-conventions.md 同步。纯前端 UI，npm run build tsc 零错误 + node --check OK。
- 2026-09-04：ActionMenuModal 补键盘 ↑↓ 选择响应——原 keydown 监听挂在 contentEl 上，但 Obsidian Modal 打开时焦点常不在其内导致上下键无反应；改为挂 document（捕获阶段、仅打开期间 attach / onClose remove），↑↓经 moveSelection 移动选中标记 ▶（跳过 disabled、到头即停不回绕），回车=确认执行、Esc=关闭；choose() 加防重入避免「回车+确认按钮/连点」双触发 onSelect。modals.ts，npm run build tsc 零错误 + node --check OK。
- 2026-09-04：**无卷模式书触发卷操作时自动转为有卷**——原先四个卷命令（按卷整理目录 / 新建卷 / 管理卷 / 打包·导出合集）在 no-vol 书上弹 NO_VOL_MODE_MSG Notice 并 return 拦截；现改为先经 main.ts 新增 `ensureVolumeModeEnabled(story)` 调 setVolumeMode(true)（仅置位、非破坏性）切到「有卷」再继续原逻辑，Notice 提示「已启用『有卷』模式，继续…」。新切换的零卷书全章留书根且未归属 → needsVolumeOrganize 恒空，ensureVolumeLayout 直接放行不误锁。story_manager 方法层 addVolume/setChapterVolume 的 NO_VOL_MODE_MSG throw 保留作兜底（正常路径已在入口前置切换、不再触发）。tsc 零错误 + esbuild 通过，release/main.js 部署 geng_bl vault（data.json 保留）。
- 2026-09-04：写字台两处 UI——①章节/卷激活 Radio「选中+悬停」态内点过大（原 scale(2)=16px 超出外环）缩小到外圆(14px)的 90%≈12.6px，即 `.aw-st-radio:checked:hover::after` transform 由 scale(2) 改 scale(1.575)×8px 基值；②书籍状态区在有卷模式下新增一行「当前卷：<名称>」（无激活卷显「无」），取 d.currentVolume→volumes 精确匹配、回退 v.active，无卷模式不占该行。styles.css + status_view.ts renderStatusLines，npm run build tsc 零错误，release/ 四文件部署 geng_bl vault（data.json 保留）。
- 2026-09-04：写字台两个顶层小节更名——「章节」（带全书章数、下挂各卷及未归属章的内容节点）→「书稿」；「全局文档」（书根参考/策划类 md：大纲/人物关系/世界观设定/写作指南等）→「案头资料」。仅改显示标签与相关注释/内置文档措辞，折叠键（chapters:/gdocs:）、命令名、「新建章节」等泛指"章节"处保持不变。status_view.ts + docs/使用说明.md + README_ZN.md，npm run build tsc 零错误，release/ 四文件部署 geng_bl vault（data.json 保留）。
- 2026-09-04：导出卷合集输入框提示词补充默认行为说明——三处入口（命令面板「导出卷合集」、管理卷菜单「导出此卷合集」、写字台卷节点右键「导出本卷合集…」）统一改为「输出路径（留空=存到该小说目录下，文件名自动为 <书名>-<卷名>-合集.md）」；仅改 prompt 文案。main.ts ×3，npm run build tsc 零错误，release/ 四文件部署 geng_bl vault（data.json 保留）。
- 2026-09-04：社区插件校验器告警修复 + README 安全声明校对——①story_manager.ts `settleTree` 的裸 `setTimeout` → `window.setTimeout`（popout 兼容）；②删除 main.ts `getSettingDefinitions()` 中未使用的 `const s`；③修正文件夹选择器深度 off-by-one：collectFolders(modals.ts) 守卫 `depth > 3`→`>= 3`，真正限制到 3 层深（与 README「descends only 3 levels deep」一致）；④README「Disclosures & Security Statement」Network 段改为三类请求（含连接测试触发的可选 `GET /models`）、api_key 留空时说明为占位值 `local-no-auth`、清除悬空的 §1 交叉引用并去掉无编号的「### 3.」标题；⑤**停止发布 WRITING_GUIDE.md**：esbuild.config.mjs 不再将其拷入 release/（文本仍由 `.md=text` 内联进 main.js、首启运行时播种系统级文件），从而消除校验器「Release contains extra unsupported files: WRITING_GUIDE.md」——同步更新 build-deploy.md / RELEASE.md / README(EN) / AGENTS.md 口径为三散文件（main.js/manifest.json/styles.css）。npm run build tsc 零错误 + node --check OK，release/ 仅 main.js/manifest.json/styles.css，部署 geng_bl vault 保留 data.json 与既有系统级指南。
- 2026-09-04：写字台章节行右键菜单顺序调整——「在本章前插入章节…」「在本章后插入章节…」两项由原 LLM 写作组之后上移为紧跟「新建章节…」之后（同属“建章”类动作），其后以分隔线接编写/续写/润色，再接重命名/删除各成一组；仅调 showContextMenu 数组项次序，无功能改动。status_view.ts，npm run build tsc 零错误 + node --check OK，release/ 三文件部署 geng_bl vault（data.json / 系统级指南保留）。
- 2026-09-04：修复「用命令面板 `/dir` 切换当前小说后、其它分栏里已打开的写字台不更新」——根因是 main.ts `notifyContextChanged()`（lastStory/current_chapter 等元数据变更的统一广播点）此前只刷新 LLM 对话面板、漏了写字台；现追加 `scheduleStatusPanelRefresh(200)` 防抖刷新所有已打开的 StatusView 叶子，使切书/切章跨分栏同步。写字台下拉框那条路本经 doSwitchStory→this.refresh() 自刷本栏，故仅跨分栏/命令入口暴露此问题。main.ts notifyContextChanged，npm run build tsc 零错误 + node --check OK，release/ 三文件部署 geng_bl vault（data.json / 系统级指南保留），workdir-behavior.md 同步。
- 2026-09-04：社区校验器类型告警清理——①去多余断言：main.ts `(p.vol ?? undefined) as string|undefined`×2、state_doc.ts `o.use_volumes as boolean`（typeof 收窄后冗余）、story_manager.ts `c.split("/")!` 对 split 结果的恒非空断言；②receiver 接受原类型的 `.catch(() => ({} as Record<…>))` 统一改为给回调显式标注返回类型（main.ts `.catch((): Record<string,string> => ({}))` ×6、story_manager `.catch((): Record<string, doc.VolumeInfo> => ({}))` ×13）——直接删断言会让 catch 把 TResult 推断成 `{}`、使 awaited 值变联合而触发 TS7053，故用返回类型注解而非裸 `{}`；③floating promise：main.ts 五处 fire-and-forget 裸调 `this.refreshVolumeSummary(...)` 加 `void` 前缀（语义不变）；④unsafe any：story_manager loadWritingData 的 `chapterRanks: Object.fromEntries(ordered.map((c,i)=>[c.key,i+1]))` 命中 fromEntries<T=any>(Iterable<any[]>) 重载致结果为 any → map 回调用 `[c.key,i+1] as [string,number]` 元组落到具体类型重载。tsc 零错误 + node --check OK，release/ 三文件部署 geng_bl vault（data.json / 系统级指南保留）。
- 2026-09-04：通用动作列表 ActionMenuModal 行排版优化——所有 `.aw-action-row` 显式 `text-align:left` 并预留固定宽度左侧标记栏（padding-left:22px），选中的非 disabled 行的 `▶` 由内联 `::before`「▶ 」改为在该栏内绝对定位（left/top:50% + translateY、去尾随空格），使各行文本起点始终一致左对齐、↑↓移动选择时不再因选中行多出 ▶ 而右移跳动；仅 styles.css 改动（章节/文件夹/小说等 SuggestModal 走 Obsidian 核心样式不受影响）。npm run build tsc 零错误 + node --check OK，ui-conventions.md 更新，release/styles.css 同步部署 geng_bl vault（data.json / 系统级指南保留）。
- 2026-09-04：通用动作列表 ActionMenuModal 选中标记改圆点并双列定宽——`.aw-action-row` 改为 flex 布局并前置两个固定宽度列：`.aw-sel`（选中的非 disabled 行经其 `::before` 显示圆点 `●`，随 `.aw-selected` 类切换、无需重渲染）与 `.aw-cur`（承载「◀ 当前」marker、无则留空占位），其后为主标签 `.aw-lab`（flex:1 min-width:0、过长换行不撑破）与说明 `.aw-sub`；两标记各自独占一列且定宽，使带/不带 marker 的行文字起点始终一致、左对齐。modals.ts renderList 重构行结构 + styles.css 重写 `.aw-action-row*` 规则，npm run build tsc 零错误 + node --check OK，ui-conventions.md 同步，release/ 三文件部署 geng_bl vault（data.json / 系统级指南保留）。
- 2026-09-04：ActionMenuModal「当前」数据侧标记方向修正——原 `"◀ 当前"`（向左实心三角）位于标签文字左侧却朝左指、背离文字，改为向右 `"▶ 当前"` 指向其后的实体名称；仅改 ActionItem.marker 六处（切换书/插入位置/管理卷·场景·人物等菜单），main.ts 中前缀 `"◀ "`（824/1015 文本行列表）与后缀 `"name ◀ 当前"`（3355 LLM provider 页名）方向各自适用、保持不变。modals.ts marker 注释示例同步为 ▶，ui-conventions.md 更新，npm run build tsc 零错误 + node --check OK，release/ 三文件部署 geng_bl vault（data.json / 系统级指南保留）。
- 2026-09-04：写字台卷节点下新增「文档」子节点——每卷展开后在章节目录之外再挂一个命名头 `.aw-st-vol-dochead`（文案「文档（N）」），列出该卷实体目录下的直属 md（非章节目录，含建卷播种的设定四件套/卷摘要等）；命名头与每个文件行右键均可「在本卷新建文档…」、文件行点击打开并可删除（缩进沿用外层 .aw-st-kids 不另造样式）。数据侧 story_manager 新增 listVolumeDocsByVol 一次性枚举各卷直属 TFile→经 StatusDetail.volumes[].docs 注入快照（view 只读渲染）；交互侧 status_view 增 renderVolumeDocs + StatusAction `new-volume-doc`，main.ts handleStatusAction 落点 `${storyPath}/${volumeFolderName(vol)}`（复用 prompt/safeFilename/vault.create）、buildStoryDetail 挂载 docs。仅显示层改动、不动仓库目录结构。status_view.ts/story_manager.ts/main.ts/styles.css，npm run build tsc 零错误 + node --check OK，ui-conventions.md 同步，release/ main.js+styles.css 部署 geng_bl vault（data.json / 系统级指南保留）。
- 2026-09-04：写字台「卷内文档」子节点可折叠 + 无卷平铺迁移保留卷内同名文档——①需求1：`.aw-st-vol-dochead` 命名头加 ▾/▸ 箭头独立开合（键 `voldocs:<书>:<卷ID>` 复用 collapsed 集合、默认展开，收起时不渲染文件行，与所在卷开合互不连动），renderVolumeDocs 重写；②需求2：切到无卷模式(/volume off)拍平时 flattenToRoot 新增 salvageVolumeDocsToRoot——把各卷残留的直属 md（设定四件套/卷摘要等）经 vault.rename 挪出到书根而非随目录进回收站，跨卷或书根已存在同名者前面加「<卷名>-」前缀避免覆盖（settleTree 保证虚拟文件系统同步），movedDocs 透出至 setVolumeMode 返回并在 cmdVolumeMode 确认弹窗文案与成功 Notice 中体现。status_view.ts/story_manager.ts/main.ts，npm run build tsc 零错误 + node --check OK，ui-conventions.md/data-model.md/command-reference.md 同步，release/ main.js+styles.css 部署 geng_bl vault（data.json / 系统级指南保留）。
- 2026-09-04：写字台「卷内文档」节点位置与点击交互调整——①固定置顶：renderVolume 里 renderVolumeDocs 调用移到章节目录循环之前，使「文档」子节点始终排在该卷第一个、各章一律置于其后；②整行开合：`.aw-st-vol-dochead` 命名头的 ▾/▸ 改为纯视觉指示符并去掉其独立 click，改由 head 整行 `addEventListener("click")` 触发 toggleCollapse+rerenderLocal（鼠标点该行任意处即展开/收起本组文档列表，避免箭头+整行双重触发）。仅 status_view.ts 改动，npm run build tsc 零错误 + node --check OK，ui-conventions.md 同步，release/main.js 部署 geng_bl vault（data.json / 系统级指南保留）。
- 2026-09-04：写字台「卷内文档」节点字体与对齐向章节样式看齐——①命名头 `.aw-st-vol-dochead` 去掉 font-size:0.9em/color:text-muted/margin-top/padding:2px8px/border-radius，改为 padding-top/bottom:3px、默认字号字色且无额外水平内边距，使其字体与文字起点同章节名行(.aw-st-chap)完全一致（悬停高亮移至 :hover、圆角随悬停出现）；②renderVolumeDocs 里文件行不再直接挂外层 .aw-st-kids，而是再嵌一层 .aw-st-kids（list）承载，使各行左缩进/对齐与「章节名下的文档」逐像素相同。status_view.ts/styles.css，npm run build tsc 零错误 + node --check OK，ui-conventions.md 同步，release/main.js+styles.css 部署 geng_bl vault（data.json / 系统级指南保留）。
- 2026-09-04：写字台「卷内文档」命名头水平位置修正——renderVolumeDocs 里 `head.appendText` 去掉多余前导空格（`` ` 文档…` `` → `` `文档…` ``），使标签紧跟 caret(自带 margin-right:0.35em)起笔、写法同章节名行(.aw-st-chap)，二者标签左缘落在同一竖线上，消除此前「文档偏一侧、章节名偏另一侧」的错位。仅 status_view.ts 改动，npm run build tsc 零错误 + node --check OK，ui-conventions.md 同步，release/main.js 部署 geng_bl vault（data.json / 系统级指南保留）。
- 2026-09-04：写字台「卷内文档」命名头水平对齐微调——按实测该命名头比同层章节名行(.aw-st-chap)靠左约一个 caret 宽，给 `.aw-st-vol-dochead` 补 `padding-left:1em` 使标签列与章节名对齐（值随主题/字号可能需再微调）。styles.css 改动，npm run build tsc 零错误 + node --check OK，ui-conventions.md 同步，release/styles.css 部署 geng_bl vault（data.json / 系统级指南保留）。
- 2026-09-05：写字台「卷内文档」节点恒存在 + 显示风格与章节名同一来源——①renderVolumeDocs 命名头改用章节名行同款类 `.aw-st-chap`（同一显示风格、不再重复定义），删除 styles.css 里旧独立规则 `.aw-st-vol-dochead`（含 padding-left:1em 对齐补丁）；②「文档」子节点改为恒渲染：无文档时也显示「文档（0）」命名头、展开后列表为空且不建空容器，renderVolume 去掉 hasContent 门槛（任何卷均可展开）。status_view.ts/styles.css，npm run build tsc 零错误 + node --check OK，ui-conventions.md 同步。
- 2026-09-05：写字台「卷内文档」命名头右键新增「补全卷文档」——检查该卷下缺失的设定模板（卷大纲/人物/人物关系/场景四件套）并按模板创建、已存在保留不覆盖；story_manager 抽出私有 volumeDocTemplates(vol) 统一建卷播种(ensureVolumeFolder)/scan 补缺(rescanStory)/新入口三处同一清单来源，新增公开 ensureVolumeDocs(story,volId) 返回本次新建名单；StatusAction 增 `complete-volume-docs`，main.ts handleStatusAction 执行并以 Notice 汇报补建名单或「已齐全」。status_view.ts/story_manager.ts/main.ts，npm run build tsc 零错误 + node --check OK，release/ 部署 geng_bl vault（data.json / 系统级指南保留），ui-conventions.md 同步。
- 2026-09-05：写字台「书籍列表」标题行右键新增「改名」（位于删除小说上方、二者间横线分隔）——改书名：manager.renameStory 更新状态文档 title → 顶层目录经 vault.rename 同步改名（safeFilename 净化后同名则仅改标题不动目录；目标重名先报错）→ 《大纲.md》起始 `# <旧书名> 大纲` 标题行精确替换（正文不动）；lastStory 指向该书时随新目录名 saveSettings（联动 LLM 面板上下文刷新）。StatusAction 增 `rename-story`，prompt 输入留空/与原书名相同不执行。status_view.ts/story_manager.ts/main.ts，npm run build tsc 零错误 + node --check OK，release/main.js 部署 geng_bl vault（data.json / 系统级指南保留），code-structure.md 同步。
- 2026-09-05：写字台「案头资料」「书稿」小节标题文字风格统一为卷名节点样式——sectionHead 对非 stories 分组改用 `.aw-st-vol` 类（与卷名行同一来源：600 字重、同款 padding/圆角/hover），`.aw-st-subsection` 去掉 font-weight 仅留纵向间距，并从 :hover 选择器移除（悬停随 .aw-st-vol:hover）；顶层「书籍列表」保持原分隔线样式不变。status_view.ts/styles.css，npm run build tsc 零错误 + node --check OK，release/main.js+styles.css 部署 geng_bl vault（data.json / 系统级指南保留），ui-conventions.md 同步。
- 2026-09-05：写字台「书稿」小节子节点增加缩进——chBody 容器补挂 `.aw-st-kids`（与「案头资料」gBody 同款处理）：卷节点/平铺章节整体相对标题缩进并带左侧指示线，体现包含关系而非与标题平齐。仅 status_view.ts 改动，npm run build tsc 零错误 + node --check OK，release/main.js 部署 geng_bl vault（data.json / 系统级指南保留），ui-conventions.md 同步。
- 2026-09-05：《写作指南汇总》空指示段自动注释——serializeAggregateGuide 各分类先经新增 prompts.commentOutEmptySections：标题下到下一同级/更高级标题间无任何内容行的小节整体包进 HTML 注释（代码围栏内 # 行不当标题、---/*** 不算内容；父节为空时含嵌套子标题一次成块），相邻空段合并一片并在上方加一条提示注释「请在 WRITING_GUIDE.md（小说级/用户级）对应章节填入写作要求」；注入前 stripComments 剥掉全部注释故不参与提示词生成，agg-hash 变更检测口径不变（变换是三层输入的确定函数）。prompts.ts，npm run build tsc 零错误 + node --check OK + 真实三层文本 esbuild 冒烟验证输出结构正确，release/main.js 部署 geng_bl vault（data.json / 系统级指南保留），data-model.md 同步。
- 2026-09-05：写作提示词上下文四项修正——①前文窗口章数 N 可配（data.json `settings.prevChapters`，设置页「前文参考章数 prevN」留空=默认 3、0=不注入；main.ts effectivePrevN 实时读取并经 StoryManagerOptions.getPrevChapters 注入）；卷模式窗口限本容器（loadWritingData 跳过落在他容器的头部），不足 N 章以阅读序在前各前卷新鲜《卷摘要》由近及远补足至 N（buildWritingContext 在【前文内容】前渲染【前卷摘要 · <卷名>】块，use_summaries=false/prevN=0 不填充）；【前文内容】标题与遍历改按实际收录章 min/max ordinal。②章节摘要自动生成对齐 CLI _summarize_chapter（prompts.buildChapterSummaryPrompt/CHAPTER_SUMMARY_SYSTEM_PROMPT 逐字一致，max_tokens=600/temp=0.4）：无正文回落本章大纲作来源（chapterSummarySource 有效来源=正文||大纲）、缺失/过期经 ensureFreshChapterSummary 调注入 LLM 生成并落盘《章节摘要.md》，存 hash 改对 md5(有效来源)校验（旧文件有正文者仍兼容）；失败回退原文预览不阻断。③五个写盘命令保存后 refreshChapterThenVolume 先刷新本章摘要再串行合并卷摘要（防读旧摘要，void 非阻塞）。④写作上下文角色范围收窄——书根《人物.md》恒含、本卷全量保留、他卷仅取卷级条目且仅限阅读序在前者（未来卷不注入、不深入他卷各章《人物.md》），场景维持 mention-driven 不变。story_manager.ts/prompts.ts/main.ts/docs/使用说明.md，npm run build tsc 零错误 + node --check OK，release/main.js 部署 geng_bl vault（data.json / 系统级指南保留），data-model.md/code-structure.md 同步。
- 2026-09-05：摘要机制改为纯延迟生成+卷摘要全量重建——①移除写盘命令后的 eager 刷新链路（refreshVolumeSummary/refreshChapterThenVolume 删除）：章节/卷摘要一律在上下文组装需要时经 manager.ensureFresh* 按需触发 LLM，失败按无摘要降级不阻断；②《卷摘要》改以该卷**全部成员章的新鲜章节摘要**为输入整体重建（prompts.buildVolRebuildPrompt 取代旧「最新章节增量合并」滚动法，消除多轮有损压缩丢失早期信息的问题），指纹口径同步改为各章「key|md5(已存新鲜摘要)」拼接 md5（与生成输入一致；旧正文哈希文件首次使用自愈重建一次），前卷缺口填充同样走 ensure* 不再静默为空；③生成过程进度反馈——StoryManagerOptions.onProgress → main.ts notifyGenProgress（单一持久 Notice 随消息更新、null 隐藏、8s 空闲超时兜底）。story_manager.ts/prompts.ts/main.ts/docs/使用说明.md，npm run build tsc 零错误 + node --check OK，release/main.js 部署 geng_bl vault（data.json / 系统级指南保留），data-model.md/code-structure.md 同步。
- 2026-09-05：摘要在途去重——story_manager.ts 新增 genInFlight Map（键「书|ch/vol|key」），ensureFreshChapterSummary/ensureFreshVolumeSummary 改薄包装+私有 doEnsure* 实现，并发交错的多个 loadWritingData 入口（写作命令预览×5、聊天面板每轮快照）共享同一生成 Promise，消除竞态窗口内同章/同卷重复 LLM 调用；任务完成即出缓存、后续组装仍走哈希门控。data-model.md/code-structure.md 同步，npm run build tsc 零错误 + node --check OK，release/main.js 部署 geng_bl vault。
- 2026-09-05：摘要按需最小化+生成过程面板——①ensureFreshVolumeSummary 改为**只读收集**本卷已有且未过期的成员章《章节摘要》作重建输入，不再遍历全卷主动触发各章 LLM 生成（缺失者跳过、待其进入前文窗口时自生；无可用则空串降级）；②进度反馈由持久 Notice 升级为专用 ItemView `src/gen_progress_view.ts`「摘要生成」面板（main.ts registerView + notifyGenProgress/getGenPanel 驱动：每任务追加一行带时间戳日志并滚底、轮末完成行、保持至下轮清空或手动关闭，新开 tab 复用已开实例，打不开仅告警不阻断），styles.css 增 `.aw-gen-*`。story_manager.ts/main.ts/styles.css/docs/使用说明.md/data-model.md/code-structure.md 同步，npm run build tsc 零错误 + node --check OK，release/main.js+styles.css 部署 geng_bl vault。
- 2026-09-05：统一生成过程面板——流式预览并入 GenProgressView（原独立 StreamingPreviewModal 删除）：①新增导出接口 WritingStreamSink（signal/fullText/done + append/reset/setStatus/finish/fail），GenProgressView 实现之，beginStream(title) 进入流式小节（移除上轮流式元素、日志行保留为历史；未决会话先按放弃收尾），Esc/「停止生成」=abort 且 done→false、关面板未决会话 resolve(false)，语义逐条对齐原 Modal；②main.ts 六个写作命令调用点（/write /continue /rewrite /polish /deai /review）改经 beginWritingPanel(title) 取同一 ItemView 实例（无则新开 tab，打不开弹通知取消），streamOnce/genFailure/streamWithEmptyRetry/generateChapterStreamed 参数类型改 WritingStreamSink；③流程体验：摘要延迟生成日志 → 「✓ 就绪」完成行 → 同面板内章节正文打字机效果 → 「保存/不保存」决定，全程一个面板。styles.css 删 `.aw-stream-modal/-body`、`.aw-gen-*` 补 status/footer 规则。src/gen_progress_view.ts/src/modals.ts/src/main.ts/styles.css/docs/使用说明.md/scope.md/ui-conventions.md/data-model.md/code-structure.md 同步，npm run build tsc 零错误 + node --check OK，release/main.js+styles.css 部署 geng_bl vault。
- 2026-09-05：写作上下文两个 bug 修复——①prevN=3 却注入整卷内容：根因是 loadWritingData 无条件把当前卷《卷摘要》（覆盖全卷章节）与跨卷前卷 digest 注入提示词，新增设置 `settings.includeVolumeSummary`（StoryManagerOptions.includeVolumeSummary 回调、设置页「注入整卷《卷摘要》」toggle 默认关）门控 volSummary/prevVolSummaries 两处注入及其 LLM 重建触发，关闭时严格只按 prevN 注入单章摘要（对齐 Python CLI context_builder 的 prev_n 语义）。②缺《章节.md》章节目录从书籍列表消失：移除 listChapters 的空心目录过滤与整个 quarantineHollowChapters 机制（方法+7 处调用点），改为磁盘上存在的章节目录一律视为存活章节（标题取自目录名、可被写作命令补写正文），重复号残留由 assertUniqueKeys 显式拒绝。story_manager.ts/main.ts/docs/使用说明.md/scope.md/data-model.md/code-structure.md/command-reference.md 同步，npm run build tsc 零错误 + node --check OK，release/main.js 部署 geng_bl vault（data.json / 系统级指南保留）。

- 2026-09-05：社区校验器告警修复——main.ts `onProgress` 回调原直接返回 async `notifyGenProgress` 的 Promise（契约要求 void），改显式 `{ void … }` 包裹；v0.1.4 发布后补丁，随下次版本生效。

- 2026-09-05：打包章节合集/导出卷合辑的章标题行补「第」前缀（「## 第二十章 虎妖初现」，对齐 Python CLI /pack）；写字台右键菜单收敛——书稿小节标题移除「新建章节…」（仅留有卷模式的「新建卷…」）、案头资料组仅保留「新建文章…」（建章走具体章节行/卷节点、建卷走书籍列表标题行或书稿小节标题）；《使用说明》升级为完整指令说明（全部命令逐条列对齐 CLI+参数/交互流程，补齐生成使用说明等缺漏项），agents/ui-conventions·code-structure·command-reference 同步。

- 2026-09-05：写字台案头资料组右键项「新建文章…」更名「新建资料…」（书根目录建资料文件；对应 prompt 标题随入口区分——书根=新建资料、章节目录内仍为新建文章）。

- 2026-09-05：写字台「书稿」小节标题右键新增「导出书稿…」——所选范围全部章节《章节.md》正文按阅读序合一 MD（留空/all=整本〔不回落 current_volume，区别于 /pack〕、区间/列表/卷名前缀支持、裸号跨容器弹选卷后重入）；有卷模式每卷组前贴「# 第N卷 · 卷名」H1 标题行（卷号按《卷.md》order 序号）、无卷平铺不带；默认输出 <书名>-书稿.md（重导覆盖）。manager 新增 packStory/packStoryByKeys；docs/使用说明 + agents/ui-conventions·code-structure 同步。

- 2026-09-05：导出书稿两问弹窗的用法说明移至输入框上方并细化（TextInputModal 增可选 hintText 第 6 参→`.aw-prompt-hint` muted 多行样式，main.ts prompt() 第 3 参透传，placeholder 只留极短占位）；装配改按容器遍历——有卷模式下**无章的空卷也照样输出其「第N卷 · 卷名」标题行**，use_volumes===false 一律平铺不带任何卷标题。

- 2026-09-05：导出卷合集的输出路径问同样把长提示移出输入框、改为输入框上方的详细 hint（命令面板 + 写字台两处右键入口共三处调用统一）。

- 2026-09-05：/pack 打包章节合集两问弹窗的长提示同样移至输入框上方并细化（范围问注明留空=当前章〔区别于导出书稿〕/all/区间/列表/卷名前缀，输出路径问列三条规则）；写字台**卷节点右键菜单新增「打包章节全集…」——置于「导出本卷合集…」之上**，复用 StatusAction export-story（整书口径，与「导出书稿」同义），形成全/单卷对照。docs/使用说明 + agents/ui-conventions 同步。

- 2026-09-05：卷节点「打包章节全集…」弹窗改为带该卷名——StatusAction export-story 增可选 volName，标题显示为「打包章节全集（卷『X』）」且范围输入框预填该卷名（直接回车=打该卷全部章节、清空后留空/all=整本）；TextInputModal 增第 7 参 initialText 预填（光标置末尾）、main.ts prompt() 第 4 参透传。「书稿」小节标题入口不带卷名、行为不变。

- 2026-09-05：导出书稿默认文件名按口径区分——**限定某卷=`<书名>-<卷名>-范围.md`**（范围取实际有正文章节号：单章 `-第N章`／连续 `-第X-Y章`／离散 `-第1、4、5章`），整本/书根域仍 `<书名>-书稿.md`；导出卷合集命名（<书名>-<卷名>-合集.md）不动。packStoryByKeys 落盘前按 scope+packed 计算文件名，输出路径问 hint 同步说明。

- 2026-09-05：写字台卷节点右键两项合并为单项「导出…」——移除「打包章节全集…」「导出本卷合集…」，只问一个范围输入框（不预填、留空/all=该卷全部章节、区间/列表按卷内本地号），不问输出路径且不支持自定义文件名；经 manager.packStory forcedVolId 锁定本卷（修 packStory：锁卷时留空=all 即该卷全章而非整本），文件首行带「# 第N卷 · 卷名」标题行、默认存 <书名>-<卷名>-范围.md。「导出书稿」（书稿小节标题入口，两问含输出路径）与命令面板/管理卷菜单的「导出卷合集」（固定整卷+ -合集命名）均不变。

- 2026-09-05：案头资料节点右键新增「补全资料」——检查书根缺失的默认资料七件套（大纲/世界观/伏笔/笔记/人物/人物关系/场景）并按模板创建、已存在保留不覆盖；manager 增 ensureRootDocs，模板清单经私有 rootDocTemplates(title) 与建书播种同一来源（newStory 改为复用之），《故事状态.md》与系统管理的《卷.md》不在其内；Notice 汇报补建名单或提示已齐全。

- 2026-09-05：写字台各级别文件列表中参与写作提示词生成的模板文档加粗显示——prompts.ts 增 PROMPT_DOCS_ROOT/VOLUME/CHAPTER 名集合（书根六件+旧别名角色关系.md、卷级四件套〔卷摘要因注入开关默认关不计入〕、章节播种六件），status_view 三处文件行命中者加 .aw-st-pdoc（700 字重+正文色）并设悬停 title「参与写作提示词生成」。
- 2026-09-05：新建资料/文档/文章优先标准模板——案头资料「新建资料…」、卷内「在本卷新建文档…」与章节块「新建文章…」改为先 pickAction 列出该容器标准模板文档（story_manager 新增 standardDocs/ensureStandardDoc，chKey/volId/书根三级解析；章节六件套提取公共 chapterDocTemplates(num,title)，建章播种与 /scan 补缺改同一来源），已存在项禁用标注、选中按模板创建不覆盖，末项「自定义文件名…」回落原名输入建空文件。
- 2026-09-05：写字台提示词参与文档标识由加粗改为主题成功色（绿）——`.aw-st-pdoc` 去掉 font-weight:700 改用 `color: var(--text-success)`，保持行内观感一致且不用链接蓝避免误认可点链接；悬停 title 说明不变。
- 2026-09-05：以上三项随 **v0.1.7** 打包发布（manifest 0.1.6→0.1.7；Gitea tag `v0.1.7` + Release 附件 articlewriter-v0.1.7.zip〔323974B，REST 校验字节数与本地一致〕+ GitHub 镜像无 v 前缀 tag `0.1.7` 触发 CI 自动重建签名上架三散文件）。
- 2026-09-05：写字台提示词参与文档标识色由绿改深灰——`.aw-st-pdoc` 改用 `var(--text-normal)`（正文色，随明暗主题适配），非提示词文档保持侧栏默认弱化灰；悬停 title 说明不变。
- 2026-09-05：新建资料/在本卷新建文档/新建文章的选择框中「自定义文件名…」调整为首项（原名输入建空文件优先可选），标准模板文档列于其后、交互与索引判定同步调整。
- 2026-09-05：写字台提示词参与文档标识色再改为主题强调色——`.aw-st-pdoc` 由 `var(--text-normal)`（深灰）改为 `var(--text-accent)`，随用户主题强调色显示；此前绿色与深灰均因部分主题下与普通文件对比不足弃用。
- 2026-09-05：写字台提示词文档标识改反向口径——未命中 PROMPT_DOCS_*（不参与写作提示词生成）的文件行着 `.aw-st-npdoc` 强调色 `var(--text-accent)`+title「不参与写作提示词生成」，标准模板文档恢复侧栏默认弱化灰正常显示；status_view 三处判定取反、旧 .aw-st-pdoc 规则移除。
- 2026-09-05：新建资料/文档/文章改单弹窗——modals.ts 新增 `NewFilePickerModal`（顶部输入框直接键入文件名回车即建空 .md〔空输入不提交〕，下方列标准模板文档行 `.aw-newdoc-opt`、已存在 `.aw-newdoc-off` 置灰禁用）+ main.ts `pickNewDoc` 辅助；new-file/new-volume-doc 两 case 不再走「pickAction 选择 + 二次 prompt」两步流程，全程只弹一次窗。
- 2026-09-05：NewFilePickerModal 改列表优先+内联输入——首屏恢复为简洁选项列表（观感同旧 pickAction、自定义置首），点「自定义文件名…」在同一弹窗内切换为输入视图并带显式「创建」「返回」按钮（手机端无回车键也可点按确认；Esc 返回列表不关窗）；仍保持单窗口不二次弹窗。
- 2026-09-05：NewFilePickerModal 再改单一视图——取消两阶段切换，首行即「自定义文件名」Setting 标签+内联输入框+CTA「创建」按钮（原地直接键入、回车或点按钮立即建文件），下方标准模板文档点击即建；空值不提交防误触。
- 2026-09-05：NewFilePickerModal 再调——去掉前置「自定义文件名」标签改为全宽输入框内部占位提示，CTA 按钮独立成行置于输入框下方并改名「确定」；列表项点击不再即时创建，仅回填名称到输入框+`.is-selected` 选中态，创建统一由「确定」触发；名称与标准文档一致时 main.ts 先 confirmBox 询问按模板空内容生成、用户确认后才 ensureStandardDoc 创建。
- 2026-09-05：NewFilePickerModal 布局定稿——「确定」按钮移至弹窗最底部（输入框→模板列表→确定），已存在项不再禁用、同样可点选；点「确定」后 std 名按存在性分支：**已存在→confirmBox 弹存在性提示（「替换」）、确认后 story_manager.overwriteStandardDoc 以模板空内容覆盖写**，不存在直接 ensureStandardDoc 创建；自定义名直接建空文件。
- 2026-09-05：修复 NewFilePickerModal 输入框未占满整行（Setting 布局在部分主题下右对齐缩窄、与下方列表错列）——改为原生 `.text-input.aw-newdoc-input`（width:100%），与模板列表同宽对齐。
- 2026-09-06：发布 v0.1.8（Gitea tag `v0.1.8` + Release 附件 articlewriter-v0.1.8.zip；GitHub 镜像 tag `0.1.8` 由 CI 自动重建签名上架）。
- 2026-09-08：状态面板右键菜单完善——「书稿」小节标题右键的「新建卷…」由「仅有卷模式显示」改为恒显示（无卷新书也能借此建卷，命令端 create-volume 会自动先切有卷模式）；卷节点右键菜单在卷名行 `.aw-st-vol` 上直接挂载（不再依赖冒泡到卷块），确保「卷名节点上右键」稳定出现「在本卷新建章节…」等本卷管理项。
- 2026-09-08：写字台「小说状态」区（`.aw-st-status`）标题行可折叠/展开——新增 `.aw-st-status-head` 可点标题行（caret + 「小说状态」标签，hover 背景提示）+ 复用 `collapsed`/`rerenderLocal` 纯展示态开合（不读盘），收起时省略信息行并去掉底部横线留白；styles.css 同步新增 `.aw-st-status-head` 与 `.aw-st-status.is-collapsed` 规则。status_view.ts/styles.css，npm run build tsc 零错误。
- 2026-09-08：「书稿」小节标题右键的新建项改为随工作模式变化——无卷（平铺）模式显示「新建章节…」（新建落书根），有卷模式显示「新建卷…」；替换原「恒显示新建卷」的口径。
- 2026-09-08：写字台「小说状态」区字数行新增含标点符号的总字符数展示——新增 `story_types.countCharsWithPunct`（计全部非空白字符，含标点/符号），`StoryManager.countWords` 每行顺带返回 `chars`（同批读取无额外 IO），`StatusDetail.totalChars` 经 getStatusSnapshot/buildStoryDetail 注入，状态行显示为「总字数 N字（含标点 M 字）」；docs/使用说明.md 同步（并补记小说状态区可折叠、书稿标题新建项随模式变化）。
- 2026-09-08：写字台「小说状态」区信息行合并——「题材/编写类型」与「当前卷」由两行并为同一行显示（当前卷仍仅有卷模式才附）。
- 2026-09-08：写字台「小说状态」区「总字数…」从章信息行拆出、单独换行显示（章数/当前章一行，总字数一行）。
- 2026-09-08：章节/卷模板文件名加名称前缀 + 旧格式自动检测迁移（修正此前「切换书籍不提示」缺陷）。新格式：章节目录内模板物理名 `<章节标题>-<基名>`（如 初见-章节.md、初见-章节大纲.md、初见-人物.md、初见-人物关系.md、初见-场景.md、初见-章节信息.md）；卷目录内 `<卷名>-<基名>`（卷大纲/人物/人物关系/场景）。story_manager 新增 `resolveChapterDocPath`/`resolveVolumeDocPath` 向后兼容解析（新格式优先、老格式裸名回退、缺失返回新名供创建），统一接入 readDoc/writeDoc/ensureDoc 及正文直读点；`chapterDocPath()` 公开给「打开章节大纲」用。新增 `detectOldFormatDocs`/`migrateOldFormatDocs`（按章/卷目录内裸基名检测→批量重命名），main.ts 在 `enforceVolumeLayoutOnSwitch` **函数开头**（早返回之前，修正原位置永不执行）调用并弹确认框。renameChapter/updateVolume 同步迁移模板文件前缀；status_view 用 `stripDocPrefix` 去前缀后比对 PROMPT_DOCS_* 着色。docs/使用说明.md、README.md 同步目录结构与说明。
- 2026-09-08：章节/卷模板文件命名再调整——由「<章名>-<基名>」改为「第NN章-章名-后缀」（正文 `第01章-初见.md`、大纲 `第01章-初见-大纲.md`、人物 `第01章-初见-人物.md`、人物关系、场景、信息；去掉“章节”二字）。卷目录同理去“卷”字：`风起-大纲.md`、`风起-人物.md`、`风起-人物关系.md`、`风起-场景.md`。story_manager 以逻辑基名读写、`chapterDocPhysicalName`/`volumeDocPhysicalName` 生成物理名；`resolveChapterDocPath`/`resolveVolumeDocPath` 三格式兼容（新格式 → 旧前缀 → 裸名）并统一接入 readDoc/writeDoc/ensureDoc 与卷文档读写点；新增 `chapterLogicalBaseOf`/`volumeLogicalBaseOf` 识别已有文件。`renameChapter`/`updateVolume`/`moveChapterDir`/`relocateChapterContainer`（重编号/插入章/换卷）均同步重命名模板文件为新前缀；`detectOldFormatDocs` 同时识别旧前缀与裸名并迁移到新格式；status_view 按逻辑基名判断 PROMPT_DOCS_* 着色。docs/使用说明.md、README.md 同步。
- 2026-09-08：章节模板文件名前缀改为纯数字章号——`第01章-初见.md` → `01-初见.md`、`第01章-初见-大纲.md` → `01-初见-大纲.md`（人物/人物关系/场景/信息同理）；**章节目录名仍为 `第01章-初见/` 不变**。`chapterDocPhysicalName` 由目录名派生数字前缀（`chapterFilePrefix`），`chapterLogicalBaseOf`/`resolveChapterDocPath` 兼容四格式（数字前缀 → 旧文件夹前缀 → 旧标题前缀 → 裸名），`detectOldFormatDocs` 改为扫描目录实际文件按逻辑基名迁移，status_view 同步。docs/使用说明.md、README.md 同步。
- 2026-09-08：修复「新建章节」失效——`createChapterAt` 建目录时误丢「第」前缀（生成 `01章-初见` 而非 `第01章-初见`），致 `listChapters` 的 `^第…章-` 正则不识别新章、面板看不到新建的章。已补回「第」前缀；并新增 `repairUnprefixedChapterFolders`（切书时把历史误建的「NN章-标题」目录自动改名为「第NN章-标题」并提示）。
- 2026-09-08：修复章节行右键「新建章节…」落到激活卷而非该章所在卷——`createItems` 增加 `volId` 参数，章节行/章节块的「新建章节」现以被右键章节所在卷为目标（缺省才走当前卷），卷节点「在本卷新建章节…」原本即以点击卷为目标。
- 2026-09-08：修复「删除中间章节后重排又多出后面的章节」——根因是 `moveChapterDir`/`renameChapter`/`relocateChapterContainer`/`updateVolume` 在 `adapter.rename`（目录改名）后立即用 `vault.getAbstractFileByPath`/`vault.rename` 改模板文件名：此时 Obsidian 索引滞后，会把旧文件名/旧路径当现状，重命名抛错中断两阶段重排，留下临时大号目录（表现为多出章节）。改为新增 `syncChapterDocFileNames`/`syncVolumeDocFileNames`，一律走真实文件系统 `adapter.list`/`adapter.exists`/`adapter.rename` 且逐个 try/catch 非致命，绝不因单文件失败中断目录级重排；`renameChapter` 的同步也移出索引依赖的 if 块。
- 2026-09-16：新增**人物关系面板**（`RelationshipView`，常驻可停靠、显示名「人物关系」，命令 `relationship-panel` + ribbon `users` 入口）：把当前小说 **书根/当前卷/当前章 三层《人物关系.md》** 汇总为关系卡片——角色对（按关系类型配 lucide 图标：师徒/敌对/恋人/亲属/主仆/门派/队友，未识别回退 link）+ 类型徽章 + 状态指示点（进行中绿/铺垫中黄/已结束灰/未标注）+ 描述原文；点击卡片或分组命名头右侧「打开文档」打开该层来源 md；顶部＝工作目录行（含「刷新」）+「小说（n）」下拉框切书 + 筛选输入框（按角色/类型/状态匹配，仅重渲染列表区以免丢输入焦点），三层分组可开合；关系文档在编辑器里新建/改动由 vault 事件经新增的 `schedulePanelRefresh()`（写字台 + 关系面板合并入口）防抖刷新。配套改动：`md_docs.ts` 新增 `RelationshipEntry`/`parseRelationships`/`formatRelationships`/`relationshipStatusKind`（兼容「`## 角色A - 角色B` + `- 类型：`/`- 状态：`/`- 描述：`」与旧「标题带角色对 + ```text 描述围栏」两种写法，字段式 `- 角色1：`/`- 角色2：` 亦可；角色对解析不出的块整块丢弃；解析↔序列化已做往返一致验证）；`story_manager` 新增公开只读 `readRelationshipDocs(story, key?)` 返回三层关系文档 `RelationshipDocSource[]`（书根带「角色关系.md」旧别名兜底）；`story_types.ts` 关系模板（`CHAPTER_RELATIONSHIPS_TEMPLATE` 亦用于书籍级 / `VOL_RELATIONSHIPS_TEMPLATE`）示例补 `类型/状态/描述` 三行；`styles.css` 新增 `.aw-rel-*`（容器/头部/滚动布局复用 `.aw-status-view` + `.aw-st-*`）。写作上下文注入口径不变（仍按 `cleanRelationshipsDoc` 原样整块注入，不做结构化改写）。docs/使用说明.md、README.md、README_ZN.md 与 agents/{code-structure,ui-conventions,data-model,command-reference}.md 同步。
- 2026-09-16：**人物关系面板头部精简为「只显示关系」**——移除工作目录行（原含「刷新」）与「小说（n）」切书下拉框，头部只剩筛选输入框 + 「刷新」按钮；`renderTop()` 不再接收快照参数（`render()` 同步改为 `renderTop()`），`.aw-rel-filter-row` 由纯 margin 改为 `display:flex` + `.aw-rel-filter` `flex:1 1 auto; width:auto; min-width:0` 让输入框与按钮同行排布。面板随之变为**纯只读视图**：`RelationshipView` 构造函数去掉 `onSwitchStory` 参数、删除 `doSwitchStory()`，main.ts `registerView` 只注入快照 getter（切书改由写字台或「切换当前小说」命令完成）；原先由头部承担的引导提示下沉到滚动区——未设工作目录→提示先执行「选择工作目录」、未选小说→提示用「切换当前小说」命令或在写字台点小说。docs/使用说明.md、README.md、README_ZN.md 与 agents/{ui-conventions,code-structure,command-reference}.md 同步。
- 2026-09-16：**人物关系面板新增「图表」展示方式**（头部「卡片 / 图表」分段按钮切换，`mode` 会话内保持、纯本地重渲染不读盘）：图表＝**手写 SVG 环形节点连线图**——`buildGraph` 把筛选后的关系行聚合为节点/边（三层同名角色合并为一个节点、同一对角色跨层重复登记合并为一条边并 `count` 计数、类型取首个非空、状态按 `active>pending>ended>unknown` 取最优先），`relationshipTypeClass` 决定连线颜色（师徒/敌对/情缘/亲族/主从/门派/同伴/其它，图例只列本次出现过的色）、`is-<kind>` 决定线型（进行中实线 / 铺垫中虚线 / 已结束点线）、多处登记加粗、无边的孤立角色用虚线圆；`focusGraph` 悬停高亮节点及其全部邻接（悬停连线则只高亮该条，其余 `.is-fade` 淡出），点节点/连线在编辑器打开来源文档；卡片与图表共用 `placeholderFor` 空态文案与同一份筛选，`renderBody` 统一分派两种渲染，切换/筛选只重渲染主体区、不重建头部（不丢输入焦点）。**不引入任何图形库**（`release/main.js` 已 1.5MB，d3 类依赖不划算），布局与高亮全本地计算。styles.css 新增 `.aw-rel-modes/.aw-rel-modebtn/.aw-rel-graph/.aw-rel-gline/.aw-rel-gedgehit/.aw-rel-gnode/.aw-rel-gname/.aw-rel-glegend*`，并删除已失去作用的 `.aw-rel-view .aw-st-section-head` 覆盖规则（那是切书下拉框时代的遗留）。验证：`npm run build`（tsc 零错误）通过；另用临时脚本（esbuild 打包 + obsidian 桩模块）对 `buildGraph` 与 `relationshipTypeClass` 做断言——同名角色合并与度数、重复登记合并为 count=2、类型保留首个非空、状态「已结束 vs 进行中」取 active、孤立角色度数 0 且入节点集、八类类型着色映射，全部 PASS（临时文件已删除）。docs/使用说明.md、README.md、README_ZN.md 与 agents/{ui-conventions,code-structure,command-reference}.md 同步。
- 2026-09-16：**人物关系新增「朋友」独立类型**（原先「友」被并入「同伴」，图例里看不到朋友）——`relationship_view.ts` 的 `TYPE_CLASSES` 把原 `is-friend`（队友|同伴|同行|盟|友|伙伴|搭档|结伴）拆为两类：`is-mate`＝同伴/队友（队友|同伴|同行|同盟|盟|结伴，边色 #7aa05a）+ `is-friend`＝朋友（友|伙伴|搭档|知己|青梅竹马，边色沿用原 #4f9b8e）；`TYPE_ICONS` 同序同理拆为 `users`（同伴）+ `user-plus`（朋友）。**顺序即优先级，同伴必须先于朋友**——「队友/盟友」含「友」字，否则会被朋友规则抢走（师徒仍先于两者，免被「徒/友」抢）；图例 `TYPE_CLASS_LABELS` 相应变为 师徒/敌对/情缘/亲族/主从/门派/同伴/朋友/其它。`story_types.ts` 关系模板的类型示例补「朋友」；styles.css 增 `.aw-rel-gedge.is-mate .aw-rel-gline` 与 `.aw-rel-glegend-swatch.is-mate`；并删除 `agents/code-structure.md` 中 `src/relationship_view.ts` 的**重复旧条目**（那条还写着已移除的工作目录行/切书下拉框）。验证：`npm run build` 通过 + 临时脚本（esbuild + obsidian 桩）断言 19 例边色（队友/同盟/盟友→is-mate，朋友/好友/挚友/知己/伙伴/青梅竹马→is-friend，师徒/宿敌/恋人/父子/主仆/同门 各归其类，空/陌生人→is-other）与 6 例图标映射全部 PASS（临时文件已删除），已部署到 vault 插件目录。docs/使用说明.md 与 agents/{code-structure,command-reference}.md 的类型枚举同步。
- 2026-09-16：**人物关系「图表」支持缩放 + 鼠标拖动平移 + 滚轮缩放（按用户要求不出滚动条）**——`relationship_view.ts` 在摘要行下方加「缩小 / 放大 / 重置视图」缩放条 + 倍率文本（档位常量 `GRAPH_ZOOMS=[0.5,0.75,1,1.25,1.5,2,3]`，`graphZoom` 与 `graphPan={x,y}` 都会话内保持、不落盘，同 `mode` 口径）：`applyView()` 只写 `svg.style.transform = translate(pan) scale(zoom)`，**不改布局尺寸**（`width:100%` / `height:auto` / `max-height:70vh` 全交给 CSS），靠 `viewBox` + `preserveAspectRatio` 让**图形与角色名文字一起等比缩放**、不重算布局、不重建 DOM、不引依赖；`transform-origin:center` 让缩放围绕图心（视觉中心不动），1 档显示「适应」。缩放入口：按钮 + **滚轮**（svg 上 `wheel` 监听，`{passive:false}` + `preventDefault` 防冒泡触发主滚动条，`deltaY` 累计 `|>=50|` 切一档、向下滑=缩小、上滑=放大）；两者共用 `zoomAt(dir, mx, my)`：**以鼠标位置为不动点**反求 pan（公式 `pan_new = m - ec - k·(m - pan - ec)`、`k=z_next/z_cur`、`ec=元素布局中心`），保证缩放前后**鼠标下的图点屏幕位置不变**——「鼠标悬停哪里、缩放就对准哪里」的标准地图手势。平移：svg 上 `pointerdown`（只认左键 + `preventDefault` 防选中文字）+ document 级 `pointermove/pointerup`（拖出面板也跟手，松手即解绑）；位移 >3px 记 `dragged` 并在**捕获阶段 `stopPropagation`** 吃掉随后的 click，避免「拖到一半松手」误打开角色/关系文档（未拖动的单击照常放行）；`applyView` 内按 `treeEl.clientWidth/Height` 与 `svg.clientWidth/Height × zoom`（新增工具函数 `clampNum`）收敛平移量，**图不会被拖出视野找不回来**。滚动条去掉：`renderBody` 切换的 `.aw-st-tree.is-graph` 由 `overflow:auto` 改为 `display:flex; flex-direction:column; overflow:hidden`（图占剩余空间并 `min-height:0` 可收缩、摘要/缩放条/图例 `flex:0 0 auto` 不被裁，避免图例被裁到面板外），放大后被裁掉的部分靠拖动查看；随之删掉 sticky 规则与 `restoreScroll()`（不再需要保持 `scrollTop/scrollLeft`），`.aw-rel-graph` 增加 `transform-origin` / `touch-action:none` / `cursor:grab` / `user-select:none` 与 `.is-panning{cursor:grabbing}`，摘要文案加「滚轮缩放（以鼠标为不动点）、按住拖拽平移」提示。画布尺寸抽为常量 `GRAPH_BASE_W/GRAPH_BASE_H`(760/560)。验证：`npm run build`（tsc 零错误）通过 + 临时冒烟（esbuild 打包 + 极简 DOM/obsidian 桩实跑 `renderBody`/`renderGraph`）断言 14 项全 PASS——回归 3 项（重置视图、按钮放大后拖动平移、松手解绑 document 监听）+ 滚轮缩放 9 项（一次到阈值放大并以鼠标为不动点更新 pan、缩放前后鼠标下局部点屏幕位置一致、单次未到阈值不切档、累计触发切档且 pan.x 撞限位、向下滚缩小、达档位上下边界再滚无效、按钮 click 也以点击位置为中心）+ 拖动限位回归与误点击拦截（临时文件已删除）。docs/使用说明.md、README.md、README_ZN.md 与 agents/{ui-conventions,code-structure}.md 同步。
- 2026-09-16：**修复新建章节时未按新格式落盘「章节号-章节名」模板文件**（`story_manager.ts` 的 `createChapterAt` 与 `rescanStory` 补全两处都直接把 `chapterDocTemplates` 返回的**逻辑基名** `["章节.md", "章节大纲.md", "人物.md", "人物关系.md", "场景.md", "章节信息.md"]` 拼成物理路径，写出的是裸名文件 `第01章-初见/章节.md` 等旧格式；后续所有读路径都走 `resolveChapterDocPath`，按"新格式优先→旧前缀→裸名"回退——只有首次写入章节正文/人物/场景内容时才会顺带创建新格式文件，`章节大纲.md` / `章节信息.md` / `人物.md` / `人物关系.md` / `场景.md` 这些无人主动写、永远停在裸名状态，所以看起来"新建章节没把这个文件也新建出来"）。修复：两处播种循环都改为 `${dirPath}/${chapterDocPhysicalName(folderName, base)}`——`folderName=章节目录名`（如「第01章-初见」），基名经 `chapterDocPhysicalName` 转 `01-初见.md` / `01-初见-大纲.md` / `01-初见-人物.md` / `01-初见-人物关系.md` / `01-初见-场景.md` / `01-初见-信息.md`，与 `resolveChapterDocPath` 的新格式优先语义对齐。旧章目录里残留的裸名文件保留不动——读取侧已经支持回退解析（line 957-960），新写入统一走新格式，跑一次 `cmdRenameChapterFile` 即可把旧章旧文件批量迁移到新格式。验证：`npm run build`（tsc 零错误）通过 + `node --check release/main.js` + 已部署到 vault 插件目录；建议手动测试：①新建第一章，确认目录 `第01章-初见/` 下应有 `01-初见.md` / `01-初见-大纲.md` / `01-初见-人物.md` / `01-初见-人物关系.md` / `01-初见-场景.md` / `01-初见-信息.md` 六个新格式文件，而非 `章节.md` 等裸名；②旧章（补丁前已建）跑 `重命名章节文件` 迁移后两种格式并存但所有读路径都能命中。
- 2026-09-16：**章节摘要/卷摘要落盘按新格式 `<编号>-<章节名>-摘要.md` / `<卷名>-摘要.md`**（同源 bug——「章节摘要.md」不在建章播种清单里（建章不主动建摘要），首次需要时由 `saveChapterSummary` 直接写 `${ch.dir.path}/章节摘要.md` 裸名；卷摘要 `saveVolumeSummary` 写 `${volDir}/卷摘要.md` 也是裸名；读路径 `readFreshSummary`/`readFreshVolumeSummary` 直读裸名——回退解析全靠 `resolveChapterDocPath`/`resolveVolumeDocPath`，但它们之前没认「章节摘要.md」「卷摘要.md」为模板逻辑基名，所以读到的就是裸名文件）。修复：把「章节摘要.md」「卷摘要.md」纳入模板清单——`CHAPTER_TEMPLATE_BASES += "章节摘要.md"`、`VOLUME_TEMPLATE_BASES += "卷摘要.md"`，并补 `CHAPTER_DOC_SUFFIX["章节摘要.md"]="摘要.md"`/`CHAPTER_SUFFIX_TO_BASE["摘要.md"]="章节摘要.md"` 与对称的卷级映射；`saveChapterSummary` 改为 `${ch.dir.path}/${chapterDocPhysicalName(ch.dir.name,"章节摘要.md")}`，`saveVolumeSummary` 改为 `${volDir}/${volumeDocPhysicalName(folder,"卷摘要.md")}`；`readFreshSummary`/`readFreshVolumeSummary` 走 `resolveChapterDocPath`/`resolveVolumeDocPath`——自动支持新格式 + 旧前缀 + 裸基名三种历史命名；`chapterLogicalBaseOf`/`volumeLogicalBaseOf` 借助 `CHAPTER_SUFFIX_TO_BASE["摘要.md"]="章节摘要.md"`/`VOLUME_SUFFIX_TO_BASE["摘要.md"]="卷摘要.md"` 反查，自动把 `01-初见-摘要.md` 识别为「章节摘要.md」——故 `detectOldFormatDocs`（扫旧文件批量迁移项）也会自动把旧「章节摘要.md」/旧「卷摘要.md」加入迁移清单，跑一次「重命名章节文件」即可批量把旧摘要迁到新格式。验证：`npm run build`（tsc 零错误）+ `node --check release/main.js` 通过 + 已部署到 vault 插件目录；`agents/data-model.md` 目录树示意同步更新为新格式文件名并标注「v0.1.4+ 延迟生成」。建议手动复测：①新建一章，待窗口章节摘要延迟生成后，目录里应有 `01-初见-摘要.md` 而非 `章节摘要.md`；②卷模式写盘若干章后，卷目录应有 `<卷名>-摘要.md` 而非 `卷摘要.md`；③旧章（旧版已生成的摘要叫「章节摘要.md」）跑一次「重命名章节文件」会一并迁移到 `01-初见-摘要.md`；④摘要内容哈希校验仍生效（哈希基于有效来源文本，正文/大纲改了会失配触发重建）。
- 2026-09-16：**关系视图：移除放大/缩小/重置按钮，鼠标滚轮缩放改为真"以鼠标为不动点"**（关系视图原在图上方挂一条工具栏「缩小/放大/重置视图」+ 倍率读数；js `zoomAt(dir, mx, my)` 用 `mx=ev.clientX`（viewport 屏幕像素）减 `ecX=svg.clientWidth/2`（layout 像素）混搭坐标系，导致滚轮缩放时"鼠标不动点"实际偏 svg 视觉中心——只有 z=1 时巧合成立，z!=1 时鼠标点会漂移；按钮 click 用 ev.clientX（按钮屏幕坐标）同样错位）。修复：①删除工具栏 DOM（`.aw-rel-gzoom`/`.aw-rel-gzoombtn`/`.aw-rel-gzoomval` 全部移除）+ 对应 CSS 样式块 + `applyView()` 内 setText/disabled 调用 + 按钮 click handler（缩小/放大/重置）；②`zoomAt` 改为接收 viewport 坐标，内部用 `svg.getBoundingClientRect()` 换算到 svg layout 偏移后与 `ecX/ecY` 同坐标系——`mx = mxView - rect.left`，公式不变（zoomAt 推到后 pan`=(1-k)(mx-ecX)+k·pan_old` 在 layout 内坐标系成立）；③提示文案已说"滚轮缩放（以鼠标为不动点）"，移除按钮后表述仍然准确，未改；④父容器 flex 规则删去已不存在的 `.aw-rel-gzoom`，只留 `.aw-rel-summary` 与 `.aw-rel-glegend`。验证：`npm run build`（tsc 零错误）+ `node --check release/main.js` 通过 + 已部署到 vault 插件目录。建议手动复测：①打开关系视图，确认图上方**不再有**"缩放：缩小 100% 放大 重置视图"工具栏；②鼠标在不同位置滚轮上下滚，图**以该鼠标点为中心**放大/缩小（不是以图中心）；③按住拖拽平移、双击图打开文档、高亮邻接等其它交互不受影响。
- 2026-09-16：**删除卷：卷目录真实文件未删除，现改为整体扔回收站**（`StoryManager.deleteVolume` 解绑回移 + `deleteVolumeCascade` 级联删章，两函数末尾都用 `vault.adapter.rmdir(volDir, false)`（非递归、只删空目录）来清卷目录，**非空就抛错被 catch 吞掉**——章节目录物理上已迁走/入回收站、卷目录内只剩杂散文件（用户手放或状态陈旧导致漏 relocate 的章节目录），rmdir 非空就保留原地 → 用户看到的现象是"卷在记录里没了，磁盘上的 `第xx卷-…/` 实体目录还在"。原本注释承认"非空残留保留原地，提示用户手动清理即可"，但实际用户没收到任何提示，体验就是"删除没生效"。修复：两处都改用 `trashFile` 模式（对齐 `clearStory` 第 1486-1487 行已用的模板）：`const vf = this.vault.getAbstractFileByPath(volDir); if (vf instanceof TFolder) { try { await this.app.fileManager.trashFile(vf); } catch { ... } }`——把卷目录整体扔 Obsidian 回收站，杂散文件跟着走（用户主动「删除卷」该有的语义），万一 trash 失败（外部持锁等极少见）才静默保留原地。函数顶 doc 注释同步更新为"卷目录整体扔回收站（含残留杂散文件）"。验证：`npm run build`（tsc 零错误）+ `node --check release/main.js` 通过 + 已部署到 vault 插件目录。建议手动复测：①/小说管理 删除某卷 → 该卷的 `第xx卷-…/` 实体目录进入 Obsidian 回收站；②写字台面板「删除本卷」右键项（级联）→ 卷目录 + 卷下全部章节一并进回收站；③Obsidian 回收站能找回删除项（trashFile 语义保证）。
- 2026-09-16：**关系图边 tip：自定义 HTML 浮层替换 SVG `<title>`，多行内容不再被浏览器原生 tooltip 截断**（关系图边悬停时浏览器原生 tooltip（Electron 内嵌 Chromium 实现）确实只显示有限高度，`desc` 长或行数多就被裁掉——SVG `<title>` 的样式/高度完全由浏览器控制，CSS 改不了，只能换实现）。改法：①移除边上 SVG `<title>` 元素；②`buildUI` 一次性创建成员 `edgeTipEl`（`this.rootEl.createDiv({ cls: "aw-rel-gtip" })`），view 关闭随父一起消失、`renderGraph` 反复重渲染时不会累积 DOM；③edge group `mouseenter` 调用 `showEdgeTip(text, ev)`（填内容 + 跟手定位 + 加 `.is-show`）、`mousemove` 调用 `moveEdgeTip(ev)` 实时跟手、`mouseleave` 调用 `hideEdgeTip()`（同时保留原有的 `focusGraph` 高亮邻接逻辑）；④tip 内容排版：元数据 4 行紧凑，描述独立空行成段，便于阅读；⑤CSS `.aw-rel-gtip`：`position:fixed` + `z-index:9999` + `max-width:320px` + `white-space:pre-wrap` + `word-break:break-word` + `pointer-events:none`（不抢鼠标事件，避免穿过 tip 时触发 edge mouseleave 导致闪掉）+ 越界自动翻向鼠标左/上避免被视口裁切。节点 tip（信息简短"X：N 条关系 + 来源路径"）仍保留 SVG `<title>`——浏览器原生 tooltip 能完整显示，且改造成本最小；如后续节点 tip 也要美化再统一替换。验证：`npm run build`（tsc 零错误）+ `node --check release/main.js` 通过 + 已部署到 vault 插件目录。建议手动复测：①打开关系视图，鼠标悬停**任一连线**，tip 浮层立即出现在鼠标右下、跟手移动、内容完整显示（元数据 4 行 + 描述独立成段，描述长就自动换行不被截）；②靠近屏幕右/下边缘悬停连线，tip 自动翻到鼠标左/上不被裁切；③离开连线 tip 立即消失，邻接高亮同步复位；④悬停**节点**仍用浏览器原生 tooltip（行为不变）；⑤节点/连线**点击**仍能打开来源文档；⑥放大/缩小、平移、悬停高亮等其它交互不受影响。
- 2026-09-16：**修复上一行引入的回归：关系图边 tip 悬停完全不出提示**（上一版把边提示从 SVG `<title>` 换成自定义 HTML 浮层后，鼠标悬停连线什么都看不到，比"内容截断"更严重）。两处根因叠加，均已修：①**`position: fixed` 被侧栏祖先"捕获"**——`edgeTipEl` 原先 `this.rootEl.createDiv`（挂在 `.aw-status-view` 内），而 Obsidian 的 `.view-content` 等祖先常带 `transform`/`contain`/`overflow`（性能优化），此时 `position:fixed` 的 containing block 会变成那个祖先而非视口、并受其 `overflow` 裁切 → tip 实际渲染在可视区之外（`left/top` 用的是 `ev.clientX/clientY` viewport 坐标，却被局部祖先裁掉）。改：`edgeTipEl` 改挂 `document.body`（`this.edgeTipEl = document.body.createDiv({ cls: "aw-rel-gtip" })`），彻底免疫祖先 transform/contain/overflow；view 关闭时在 `onClose` 手动 `remove()` 并置 null；成员类型改 `HTMLDivElement | null`，`showEdgeTip`/`moveEdgeTip`/`hideEdgeTip` 全部加空值守卫。②**定位前读尺寸恒为 0**——原 `showEdgeTip` 顺序是 `setText → moveEdgeTip → addClass("is-show")`，`moveEdgeTip` 里读 `offsetWidth/offsetHeight` 时元素还是 `display:none`，读到 0 → "右侧/下方越界翻到左/上"的翻转判断全部失效 → 长 tip（元数据 4 行 + desc 段落）直接从鼠标右下方延伸出视口底部，几乎看不到。改：`showEdgeTip` 顺序调整为 `setText → addClass("is-show") → moveEdgeTip`（先显示才能读到真实尺寸）；`moveEdgeTip` 加 `is-show` 守卫，未显示时不定位。验证：`npm run build`（tsc 零错误）+ `node --check release/main.js` 通过 + 已部署到 vault 插件目录（CSS 上一版已含 `.aw-rel-gtip`/`.aw-rel-gtip.is-show`，本次未改）。**注意需重载插件（禁用/启用 ArticleWriter 或 Ctrl+P → Reload app without saving）才会加载新 main.js**。建议手动复测：①重载插件后打开关系视图，鼠标悬停任一连线 → tip 在鼠标右下角完整显示（元数据 4 行 + 描述独立成段）；②移到屏幕右/下边缘悬停 → tip 自动翻到鼠标左/上不被裁；③离开连线 tip 立即消失、邻接高亮复位；④关闭关系视图面板后检查 `document.body` 内不残留 `.aw-rel-gtip` 元素；⑤节点悬停（原生 tooltip）、点击打开文档、缩放/平移仍正常。
- 2026-09-16：**人物关系面板：打开文档改为双击（单击不触发）**（用户反馈单击易误触误开文档）。改动：`src/relationship_view.ts` 三处 `click` → `dblclick`——卡片 `renderCard`（含 `title` 文案改「双击在编辑器中打开来源文档」）、图表边 group、图表节点 group；svg 上拖拽后的捕获阶段拦截同时覆盖 `click` 与 `dblclick`（`dragged` 标记语义不变，拖到一半松手不会误开）；分组头右侧「打开文档」是明确按钮、**保留单击**；文案同步更新（图表摘要行「双击节点/连线打开来源文档」、节点 `<title>`、类注释与 renderCard/renderGraph 方法注释）。
- 2026-09-16：**人物关系面板新增「添加人物」按钮 + 弹窗（写人物与关系文档）**（需求：在面板上新增按钮 → 弹窗填人物信息 + 选归属级别〔书籍级/卷级/章级〕→ 「添加关系」动态新增关系行〔对象下拉 = 该级别范围内人物优先、类型下拉 = 支持的关系类型〕→ 可加多行 → 底部「确定」→ 写对应《人物.md》与《人物关系.md》）。实现：①`src/modals.ts` 新增 `AddCharacterModal`（导出 `CharacterScopeOption` / `CharacterRelationInput` / `AddCharacterResult`）：单页表单＝姓名 + 人物级别下拉（`onChange` 即重建关系区）+ 身份/年龄/性别/性格/外貌/背景/能力/备注 + 关系区（每行＝对象下拉 + 类型下拉 + ✕ 删除，「+ 添加关系」可加多行，切换级别时旧选值不在候选里会补 option 防丢）+ 底部「确定」CTA（姓名空则 Notice 不提交）；**Modal 只回数据、不碰 vault**。②`src/story_manager.ts`：`characterFilePath`/`saveCharactersFile` 支持**卷级人物**（`chapter=0 && vol` → 落 `resolveVolumeDocPath(卷目录, 卷名, 人物.md)`、H1 用卷名，与 `loadAllCharacters` 既有卷级读取口径一致），`addCharacter` 相应放开 `chapter=0 + vol`（此前该组合被强制丢弃 vol 落书根）；新增 `appendRelationships(story, scope, entries, {volId?, chapterKey?})`——把 `RelationshipEntry[]` **追加**到指定层级的《人物关系.md》（书根/卷目录/章节目录），文档缺失或为空时按该层模板起底（书/章 `CHAPTER_RELATIONSHIPS_TEMPLATE`、卷 `VOL_RELATIONSHIPS_TEMPLATE`）、已有内容原样保留，返回写入路径。③`src/relationship_view.ts`：构造函数新增可选第 3 参 `onAddCharacter`，头部「刷新」前加「添加人物」按钮（未选小说时 disabled）。④`src/main.ts`：`registerView` 注入 `() => this.openAddCharacterDialog()`；新增 `openAddCharacterDialog()`——`activeStory()` + `loadAllCharacters` 按层级算候选（书级=书根全局、卷级=该卷卷目录、章级=该章目录）→ 级别选项含「书籍级 / 各卷 / 各章」并把**默认项置顶**（优先当前章 → 当前卷 → 书籍级，贴合当前写作位置）→ 弹 `AddCharacterModal` → 提交后 `manager.addCharacter`（章级 `parseChKey` 取 num/vol、卷级 `chapter=0 + volId`、书籍级 `chapter=0` 无 vol）+ 关系非空时 `manager.appendRelationships`（status 默认 `active`）→ Notice + `schedulePanelRefresh(300)`；模块常量 `RELATION_TYPE_OPTIONS`＝师徒/同门/朋友/同伴/恋人/亲族/主仆/敌对/对手/盟友（均能命中 relationship_view 的类型正则，保证图视图边色与图标可识别）。⑤`styles.css` 新增 `.aw-addchar-*`（关系行 flex 布局、下拉可收缩、底部右对齐 CTA）。⑥文档同步：`docs/使用说明.md` 关系面板段（双击打开、移除缩放条描述、新按钮用法）、`agents/ui-conventions.md`（Modal 表新增 `AddCharacterModal` 行 + RelationshipView 段落改为「注入 getter + 可选 onAddCharacter」/双击/移除缩放条/`onClose` 清理 body 上的 tip）、`agents/code-structure.md`（relationship_view / story_manager / main.ts / modals.ts 四行）、`agents/data-model.md`（卷级人物落盘口径 + `appendRelationships` 写入入口）。验证：`npm run build`（tsc 零错误）+ `node --check release/main.js` 通过 + 已部署。**需重载插件生效**。建议手动复测：①面板头部出现「添加人物」，未选小说时禁用；②点开填姓名、选「卷级 · 某卷」+ 两条关系 → 确定 → 该卷目录《人物.md》出现角色、《人物关系.md》追加两条 `## 角色 - 对象` + `- 类型：` + `- 状态：active`；③选「章级」默认落在当前章；④关系行可加多行、可删；⑤双击卡片/节点/连线才打开文档、单击无反应；⑥文件名含非法字符的姓名被 `safeFilename` 净化。
- 2026-09-16：**「添加人物」弹窗属性行间距压紧**（用户反馈属性字段逐行间隔偏大）。`AddCharacterModal.onOpen` 首行给 contentEl 加专属类 `.aw-addchar-modal`，styles.css 新增三条覆盖：`.aw-addchar-modal .setting-item { padding: 3px 0 }`（Obsidian 默认 0.75em 约 12px，压到 3px）、`.setting-item-info { margin-bottom: 0 }`（去掉名称与控件之间的额外下边距）、`h4 { margin: 10px 0 2px }`；关系行列表 `.aw-addchar-rels` 的 `gap` 由 6px 收到 4px。样式全部限定在 `.aw-addchar-modal` 作用域内，不影响其它 Modal 与设置页。验证：`npm run build`（tsc 零错误）+ `node --check release/main.js` 通过 + 已部署（styles.css 与 main.js 均确认含 `aw-addchar-modal`）。需重载插件后生效。
- 2026-09-16：**多字段弹窗分隔线口径修正 + 紧凑样式生效加固**（用户反馈：属性行之间的分隔线要去掉、只在「人物关系」上方保留一条；且上一版紧凑样式观感没变化）。①紧凑类由 `AddCharacterModal` 专用改为**通用 `.aw-form-compact`**，`MultiFieldModal`（「新增人物 / 编辑人物信息」等 4 处调用）与 `AddCharacterModal` 的 onOpen 都加该类——上一版只加在 AddCharacterModal 上，用户若在「新增人物」命令的表单里看则完全不受影响。②分隔线：把选择器从 `.aw-form-compact .setting-item` 提高到 `.aw-form-compact .setting-item:not(:first-child)`（特异性 0,3,0）——Obsidian 内置规则正是 `.setting-item:not(:first-child) { border-top: … }`（0,2,0），只写前者时与之同级、胜负取决于注入顺序，是"改了没效果"的典型原因；现**属性各字段行之间零分隔线**。③**唯一保留的横线**给 `.aw-form-compact h4`（＝「人物关系」小节标题）`padding-top: 8px + border-top: 1px solid var(--background-modifier-border)`，正好把属性区与关系区分开。④其余压紧：`.setting-item` padding→1px、`.setting-item-info` margin/padding→0 且 max-width 42%、`.setting-item-name` 0.9em/1.35、`.setting-item-control` padding→0、文本输入与下拉高 24px、按钮高 24px、h3 margin→0 0 6px；末尾按钮行用 `.aw-form-compact > .setting-item:last-child:not(:first-child)`（0,4,0，避免被上面 0,3,0 的 padding 简写覆盖）留 6px 呼吸。作用域仍严格限定在这两个 Modal 的 contentEl，不外溢设置页/其它弹窗。验证：`npm run build`（tsc 零错误）+ 已部署（styles.css 12 处、main.js 2 处含 `aw-form-compact`）。**必须重载插件**（设置 → 第三方插件 → 关闭再打开 ArticleWriter，或 Ctrl+P「Reload app without saving」）才会重新加载 styles.css。
- 2026-09-16：**多字段弹窗输入框撑满「标签之外的一整行」**（用户要求：输入框吃满除文字标签外的剩余宽度）。Obsidian 默认 `.setting-item-control` 是 `flex: 0 0 auto` + `justify-content: flex-end`，`addText` 的输入框只按内容宽度靠右摆、右侧留大片空白。改法：`.aw-form-compact .setting-item-control { flex: 1 1 auto; min-width: 0; padding: 0; justify-content: stretch }`（标签侧 `.setting-item-info` 仍是 `flex: 0 0 auto` + `max-width: 42%`——短标签自然只占自身宽度，输入框即得剩余全部空间），并给 `.setting-item-control input[type="text"]` 与 `select` 加 `flex: 1 1 auto; width: 100%; min-width: 0`（配合已有 `height: 24px`），`.aw-form-compact` 作用域内所有文本输入与下拉统一满宽（末尾「确定/创建」按钮不设 width、不受影响；关系行内的对象/类型下拉在 `.aw-addchar-rels` 内本已 `flex: 1`，不受这条规则影响）。验证：`npm run build`（tsc 零错误）+ 已部署（styles.css 已含 `justify-content: stretch`）。**需重载插件**后生效。
- 2026-09-16：**多字段弹窗：标签与输入框之间加 10px 空隙**。在 `.aw-form-compact .setting-item` 上加 `gap: 10px`（该行是 flex 两列布局——标签 `.setting-item-info` 与控件区 `.setting-item-control`，`gap` 只作用在两者之间，不影响其它间距）。验证：`npm run build`（tsc 零错误）+ 已部署（styles.css 已含 `gap: 10px`）。需重载插件后生效。
- 2026-09-16：**多字段弹窗：下拉框与输入框左右边缘逐行对齐（标签列改定宽）**（用户反馈「人物级别」下拉框与其它输入框没对齐）。根因：`.setting-item-info` 采用 `flex: 0 0 auto`（按内容宽度）时，「人物级别」这类 4 字标签比「身份/年龄」等 2 字标签宽 → 每行控件区（`.setting-item-control`）起点不同，虽然右边缘都被 `flex: 1 1 auto` 顶到容器右侧，但下拉/输入框的**左**边缘逐行错位、宽度也不一致。改法：`.aw-form-compact .setting-item-info` 定为 `flex: 0 0 6.5em; max-width: 6.5em`（标签列定宽 ≈104px，容纳「人物级别」「能力/技能」等），`.setting-item-name` 加 `white-space: normal; overflow-wrap: anywhere` 让超长标签（如 MultiFieldModal 的「归属章节号（卷内本地号）」）**在本列内换行**而不是撑宽列；配合既有 `.setting-item-control { flex: 1 1 auto }` 与 `input/select { width: 100% }`，各行输入框与下拉框左右边缘完全对齐。验证：`npm run build`（tsc 零错误）+ 已部署（styles.css 含 2 处 `6.5em`）。需重载插件后生效；若 6.5em 与你的界面字号不匹配（标签换行过多或留白过多）可再微调。
- 2026-09-16：**「+ 添加关系」按钮右对齐到与输入框同一条右缘**（用户要求按钮放到右边）。根因：上一版为了让输入框撑满，把 `.setting-item-control` 的 `justify-content` 从 Obsidian 默认的 `flex-end` 改成了 `stretch`（row 方向上它退化为 flex-start），于是 `.setting-item-control button` 失去右靠行为、被挤到左侧。改法：`.aw-form-compact .setting-item-control button { margin: 0 0 0 auto }`——用 auto 左边距把按钮顶回控件区右缘，而控件区右缘就是上方输入框/下拉框的右缘，故**按钮与输入框自动对齐**；同一规则同时修好 MultiFieldModal（「新增/编辑人物」等）末尾「创建」按钮行被挤到左侧的连带问题。底部「确定」按钮在 `.aw-addchar-actions`（非 setting-item，flex-end 本来就右对齐）不受影响。验证：`npm run build`（tsc 零错误）+ 已部署（styles.css 含 `margin: 0 0 0 auto`）。需重载插件后生效。
- 2026-09-17：**修复写作命令报连接错误（`Connection error.`）：流式/生成类请求超时由 SDK 默认 60s 放宽到 30 分钟 + LLM 错误诊断信息补全**（用户反馈：LLM 对话框能正常对话，但走「编写本章…」时报 connect err）。排查：`main.ts` 的写作命令与 `llm_chat_view` 用的是同一个 `chatStream(cfg, messages, cb, undefined, signal)`、同一端点同一 key，**唯一实质差异是生成时长与请求体量**——对话框短问答秒级完成，而整章生成（含三层上下文 + 创作规范 + 前文窗口）常远超 openai SDK `createClient(cfg)` 的默认 60s，超时被 SDK 掐断后统一表现为连接类失败。改法：①`llm_client.ts` 新增导出 `GENERATE_TIMEOUT_MS = 30 分钟`，`chatStream` 与 `chatCompletion` 都改用它（`createClient(cfg, GENERATE_TIMEOUT_MS)`）——正常中断改由流式面板「停止生成」的 AbortSignal 负责，不再依赖超时（`testConnection` 仍保持 20s 快速失败）；②新增导出 `describeLlmError(e)`：把 SDK 异常的 `message` + `HTTP status` + `code` + `type` + 底层 `cause.code/message` 拼成一行（SDK 的 `APIConnectionError` 原始文案只有 "Connection error."，不带 cause 时无法区分「连接被拒 / 超时 / 4xx / 服务端断开」），并对 `Bearer …`、`sk-…` 做脱敏；`main.ts` 的 `notifyError`、`genFailure`（写入生成面板失败态）与 `llm_chat_view` 的请求失败提示统一改走它，Notice 时长 6s→8s；③`cmdWrite` 生成前加 `console.debug("[articlewriter] 编写本章请求规模", { model, systemLen, promptLen })` 便于判断是否与请求体量相关。验证：`npm run build`（tsc 零错误）+ `node --check release/main.js` 通过 + 已部署。**需重载插件后复测**；若仍失败，新提示会带出 `cause=ECONNRESET/ECONNREFUSED/ETIMEDOUT` 等具体码，可据此继续定位（本地模型并发限制 / 代理拦截 / 网关请求体上限）。
- 2026-09-17：**写作命令连接失败（DeepSeek，`cause=Failed to fetch`）自动退避重试**（承接上一行：用户复测后给出精确诊断串）。`Failed to fetch` 是 fetch 在网络层失败时的统一报错（不带 HTTP 状态），在 DeepSeek 场景下最典型的成因是**长 prompt 首字节等待过久、连接被服务端网关/中间设备提前掐断**，其次是并发占满或代理不稳。新增 `llm_client.isRetryableLlmError(e)`：只放行「网络层瞬时故障 + 5xx / 408 / 429」可重试，参数类 4xx（超长上下文、密钥错、模型名错等）直接失败不白等（判定依据 `status` 优先，其次匹配 message/cause 里的 `failed to fetch / connection error / ETIMEDOUT / ECONNRESET / ECONNREFUSED / socket hang up / timed out / 502/503/504`）；`main.ts` 的 `streamOnce` 改为最多 3 次尝试：失败且可重试时 `sink.setStatus("连接中断，N 秒后自动重试…")` + `sink.reset()` 丢弃半截输出 + 退避 2s / 4s 后重发（用户中断优先，`sink.signal.aborted` 立即抛出）。验证：`npm run build`（tsc 零错误）+ `node --check release/main.js` 通过 + 已部署。**需重载插件后复测**。若重试仍全失败，用控制台 `[articlewriter] 编写本章请求规模` 的 systemLen/promptLen 判断是否请求体过大（对策：设置页把「前文参考章数」降到 0–1、精简三层写作指南与文档）。
- 2026-09-17：**修复写作命令"一发请求就报 `Failed to fetch`"：发送前清洗提示词里的孤立代理码元（lone surrogate）**（用户澄清：不是被掐断，而是**一点开始写就立即报错**；且同一 `chatStream` 在 LLM 对话框里能正常回复，与代理/网络无关——这排除了超时与网络层，指向**客户端侧请求体编码失败**）。根因：`fetch` 把字符串 body 转成 USVString（UTF-8）时，遇到**未配对的 UTF-16 代理码元**（如残缺 emoji 的 `\uD83D` 单独出现、从网页/Word 复制粘贴进《章节大纲》《人物》《写作指南》等文档的脏文本）会**当场**抛 `TypeError: Failed to fetch`，既无 HTTP 状态也无 cause 细节；对话框与写作命令注入的文本不同，故只在写作命令（章纲原文 + 前文窗口 + 三层 folderDocs 全量）命中。改法：`llm_client.ts` 新增导出 `sanitizeText(s)`（快速通道正则预判 + 逐码元扫描，合法代理对〔正常 emoji〕原样保留，孤立代理替换为 U+FFFD）与内部 `sanitizeMessages()`，并在 `buildParams` 里统一清洗 messages（`chatStream` 与 `chatCompletion` 一次覆盖：写作命令、续写/润色/重写/审阅、去AI味、摘要与卷摘要生成、LLM 对话框全部生效）；命中时 `console.warn` 打印清洗条数 + **坏字符前后 40 字符的片段**（便于定位是哪份文档带进来的脏文本）。验证：`npm run build`（tsc 零错误）+ `node --check release/main.js` 通过 + 已部署（release/main.js 含 2 处 `sanitizeText`）。**需重载插件后复测**；若控制台出现该 warn，按片段定位并清理来源文档里的残缺 emoji 即可根治。
- 2026-09-17：**定位并修复「编写本章」连接失败真因：激活模型指向未启动的本地服务（与对话框所选配置不一致）+ 三处防呆**（上一轮的孤立代理码元清洗保留为通用加固，但本次实测证明不是该原因）。诊断过程：读 vault 的 `data.json` 发现 `llm.active_llm = "local"`（`base_url = http://localhost:8509`、无 key），而 `deepseek` 配置存在且 URL/密钥正常；进一步 `curl http://localhost:8509/v1/models` 实测 **连接被拒**（`Failed to connect ... after 0 ms`、无任何进程监听 8509）——写作命令一律走设置页「激活模型」（`getLlmSetup`），故请求本地 8509 被立即拒绝，`fetch` 报 `Failed to fetch`（无 HTTP 状态、瞬间失败）；而 LLM 对话框顶部下拉可**另选配置**（用户在那里选了 deepseek）所以能正常回复——这就是"同一函数、与代理无关、却只有写作命令失败"的原因。代码改动：①`main.ts` 新增 `decorateLlmError(e, cfg)`：连接类失败（Failed to fetch / Connection error / ECONNREFUSED / ETIMEDOUT / ECONNRESET / socket hang up）时在错误里补上**实际使用的端点与模型**，并提示"本地地址请确认服务已启动；写作命令用设置页「激活模型」，与对话框下拉可能不同"；`streamOnce` 抛出前统一走它。②`streamOnce` 对**本地端点（localhost/127.0.0.1/0.0.0.0/[::1]）跳过自动重试**——服务没起来时重试必然同样失败，直接报错让用户看到端点更省时间（云端端点仍保留 2s/4s 退避重试）。③`beginWritingPanel` 把**写作命令实际使用的激活模型名**拼进生成面板标题（如「创作 第一卷第03章 初见 · 模型 local」），避免用户误以为与对话框用的是同一个模型。④`llm_client.buildParams` 增加已知端点上限保护：`base_url` 含 `api.deepseek.com` 且 `max_tokens > 8192` 时自动夹取为 8192 并 warn（该用户配置里是 65535，切到 DeepSeek 后原本每条命令都会被 400 拒绝）。验证：`npm run build`（tsc 零错误）+ `node --check release/main.js` 通过 + 已部署（release/main.js 含 2 处 `decorateLlmError`）。**用户侧修复**：设置 → ArticleWriter → 把「激活模型」切到 deepseek（或启动 8509 的本地服务）；同时建议把 max_tokens 调到 8192 以内。
- 2026-09-17：**LLM 对话框与「激活模型」双向同步（选择即持久化）**（用户要求：对话框改选后记录下来写进 data.json、下次启动加载，且「编写本章」等命令立刻用最新激活模型）。此前对话框顶部「模型：」下拉只在内存里改 `activeIdx`（`llm_chat_view.ts` 的 change 回调只 `updateModelLabel()`），而写作命令/连接测试一律读设置页的 `active_llm`——两者是**两套状态**，这正是"对话框能聊、编写本章连不上（激活项还指着没启动的本地 local）"的根源。改动：①`LlmChatView` 构造新增第 5 参 `setActiveModel?: (name) => void | Promise<void>`，下拉 change 时用当前配置名回调它；②`main.ts` 新增 `setActiveLlmFromChat(name)`：写 `settings.llm.active_llm` + `saveSettings()`（落 data.json）+ Notice「已切换激活模型：X（写作命令将使用它）」——写作命令每次经 `getLlmSetup()` 实时读该字段，故**下一次「编写本章」立即用新模型**，重启后按它加载；③新增 `syncChatModelSelect()` 遍历已打开的 LLM 面板调其下拉重建，并在设置页「当前激活配置」`setControlValue`、「设为激活」`setActiveLlm`、`addLlmConfig`、`removeLlmConfig`、`reorderLlmConfigs` 五处调用（反向同步）；④`LlmChatView.refreshModels()` 改为 **public** 且**选中项一律以 `active_llm` 为准**（移除原"优先保持原选择"分支——切换会写回激活项，故保持选择的效果自然成立，同时设置页改动也能立刻体现，两边永远一致）。文档同步：`docs/使用说明.md`（对话框段说明"此处切换即设为激活"）、`agents/ui-conventions.md`（LlmChatView 段落）、`agents/code-structure.md`（llm_chat_view 行 + main.ts 行的 `openLlmPanel` 片段）、`agents/data-model.md`（LLM 配置段补「激活项双向同步」）。验证：`npm run build`（tsc 零错误）+ `node --check release/main.js` 通过 + 已部署（release/main.js 含 8 处相关符号）。**需重载插件**；注意你当前 data.json 的 `active_llm` 仍是 `local`（localhost:8509 无服务），重载后对话框下拉会显示 local，改成 deepseek 即会写回并即刻生效。
- 2026-09-17：**修复「切书时自动开始写」：只读展示路径不再触发摘要 LLM 生成**（用户反馈：切书时自动开始写新章节了）。排查：切书链路本身干净——`cmdSwitchStory`/`statusSwitchStory` 只写 `lastStory` + `enforceVolumeLayoutOnSwitch`（目录名修复、旧格式文档迁移询问、平面残留整理引导，均无建章/写作），`activeStory`/`statusActivateChapter` 亦无。真凶在 **LLM 对话面板的「提示词」标签刷新**：切书/切章会经 `notifyContextChanged()`（由每次 `saveState` 触发）调用 `LlmChatView.updateSpLabel()` → `main.ts getChatSystemPrompt()` → `manager.loadWritingData(...)`，而该方法为取窗口章摘要**无条件走 `ensureFreshChapterSummary`**（缺失/过期即**发起 LLM 调用并落盘**）、卷摘要同理——于是每次切书都凭空发起 LLM 生成、`onProgress` 顺带把「生成过程」面板顶出来并开始输出，用户视角就是"自动开始写"。改法：①`story_manager.loadWritingData` 的 `opts` 新增 **`ensureSummaries?: boolean`（默认 true）**：为 false 时窗口章摘要改用已有的**只读** `readFreshSummary`、前卷/本卷摘要改用**只读** `readFreshVolumeSummary`（两者本就存在：读文件+校验指纹，不新鲜即返回空串），全程零 LLM 调用、零落盘；②`main.ts getChatSystemPrompt()` 传 `{ includeCurrentSummary:false, ensureSummaries:false }` —— 该函数同时服务「提示词字数标签」与「对话框每轮系统提示词」，两处都不该有写操作副作用（对话框是辅助问答，不该因聊天烧 token 生成摘要）；③写作命令（/write /continue /rewrite /polish /review 共 5 处 `loadWritingData` 调用）保持默认 true，摘要延迟生成语义不变。文档同步 `agents/data-model.md`（v0.1.4+ 条③补 `ensureSummaries` 口径）。验证：`npm run build`（tsc 零错误）+ `node --check release/main.js` 通过 + 已部署（release/main.js 含 5 处 `ensureSummaries`）。**需重载插件**；复测：切书/切章/任何状态变更时「生成过程」面板都不应再自动弹出，需摘要时仍由写作命令按需生成。
- 2026-09-19：**人物关系面板图表模式新增「人物状态」区**：单击角色节点选中它（`selectedChar` 会话内保持、筛选后不在图内自动清除；拖拽平移后的伪点击被 svg 捕获阶段 click 拦截不误选），图表主体底部固定显示该人物的关系详情——名称＋关系条数、「来源：打开文档」链接（涉及行 sourcePath 去重）、× 清除按钮，其下逐条复用 `renderCard` 渲染关系卡片（类型徽章/状态点/描述，双击开源 md）；未选中时给引导文案；选中节点加 `.is-sel` 强调色描边。同步更新 `docs/使用说明.md`、`agents/code-structure.md`、`agents/ui-conventions.md`。验证：`npm run build`（tsc 零错误）+ `node --check release/main.js` 通过 + 已部署。**需重载插件**。
- 2026-09-19：**人物关系面板「人物状态」区改为展示《人物.md》角色设定**（用户反馈：原实现显示的是关系列表，应改为人物信息）。数据链路：`story_manager.ts` 新增导出接口 `CharacterInfoEntry extends doc.CharacterDoc`（＋scope/label/sourcePath）与只读方法 `loadCharacterEntries(storyName)`——遍历书根＋每一卷＋每一章的《人物.md》逐层 `parseCharacters`（与 loadAllCharacters 同口径但保留每条出处、同名多层不合并；单层解析失败静默跳过），main.ts `getRelationshipSnapshot` 将其映射进快照新字段 `chars: RelCharInfo[]`（读取失败不阻断关系展示，三处早退返回补默认空数组）；`relationship_view.ts` `renderCharInfo` 重写为按名精确匹配 `chars` 后逐条经新方法 `renderCharEntry` 出块＝层级标签＋「打开文档」链接＋身份/年龄/性别/性格/外貌/背景/能力（顿号连接）/备注字段行（未填项跳过），查无此角时提示用顶部「添加人物」补录（移除旧的关系卡片列表与来源去重逻辑）；styles.css 以 `.aw-rel-charentry/.aw-rel-charenty-head/.aw-rel-charscope/.aw-rel-charfield/.aw-rel-charflabel` 替换原 `.aw-rel-charsrcs/.aw-rel-charlist`。同步更新 `docs/使用说明.md`、`agents/code-structure.md`、`agents/ui-conventions.md`。验证：`npm run build`（tsc 零错误）+ `node --check release/main.js` 通过 + 已部署。**需重载插件**。
- 2026-09-20：**每本书独立工作区**（用户要求：给每本书增加以书籍名字命名的工作区，切换/加载书籍时加载此工作区）。载体＝插件数据目录裸文件 `.obsidian/plugins/articlewriter/workspaces/<书名>.json`（内容 `{v, platform, layout}`，layout 即 `workspace.getLayout()` 全量布局对象；走既有 DataAdapter 裸 IO，不被索引收录）。main.ts 新增四方法：`bookWorkspacePath(story)`／`captureBookWorkspace(story)`（尽力存档，失败仅 warn）／`loadBookWorkspace(story)`（`changeLayout` 恢复；无记录/桌面↔移动跨端/JSON 损坏均静默保持现状，成功弹「已加载『X』的工作区」Notice）／**`applyStorySwitch(next)` 统一入口**（先归档旧书当前窗口状态 → 写回 lastStory+saveSettings → 恢复新书工作区；重选同一本书为无操作）——所有改 lastStory 的路径一律收敛于此：cmdSwitchStory、statusSwitchStory（写字台下拉框）、activeStory 单本自动选中与多本选择器回调、pickWorkDir 唯一书自动加载（清空记忆前先归档旧书）、cmdNewStory（激活新书前归档原当前书）、statusRunWriting 右键写作切书、设置页 setControlValue("workDir") 直接改目录（异步归档）；rename-story 把该书存档文件随改名迁移、delete-story 清理其存档。另订阅 workspace `layout-change` 防抖 1500ms 自动把布局存入当前书（3s 时间窗过滤自身 changeLayout 的回声事件）。Obsidian 重启仍按全局会话恢复上次布局（即最近一次已存档状态），插件不额外干预。同步 docs/使用说明.md（常驻面板节新增条目）。验证：npm run build（tsc 零错误）+ node --check release/main.js 通过 + 已部署。**需重载插件**。
- 2026-09-20：Lint 修复——llm_client.ts sanitizeMessages 中 `{ ...m, content: cleaned } as Message` 的断言冗余（展开对象本即可赋给 ChatCompletionMessageParam，触发 no-unnecessary-type-assertion），直接移除；构建产物字节级不变（esbuild 本就擦除类型断言），无需重新部署。验证：npm run build（tsc 零错误）+ node --check 通过 + md5 对照一致。
- 2026-09-20：每本书工作区功能加诊断日志（用户反馈切书未见生效，且 vault 中 workspaces/ 目录不存在＝捕获从未执行）——applyStorySwitch/captureBookWorkspace/loadBookWorkspace 各决策点补 console.info/warn 追踪（切书入口、存档成功路径、无存档/跨端/损坏/layoutReady 未就绪等分支均可见），便于 DevTools 控制台定位；已重新构建部署。
- 2026-09-20：每本书工作区排障——用户收到「存档目录不可写」通知（证明新代码在跑但 DataAdapter 相对路径写入 .obsidian/... 抛错；bash 直写同路径成功，系 Obsidian adapter 行为差异）。修复与加固：①story_manager.writePluginFile 桌面端失败时经 FileSystemAdapter.getFullPath 取 vault 根绝对路径重试一次（运行时类型守卫，基类 dts 未声明该方法）；②main.ts 新增 noteWsIoError 统一上报——console.error 全量细节＋Notice 带真实报错文本且每会话仅弹一次；心跳/capture/load 四处 catch 收敛到它。验证：npm run build tsc 零错误 + node --check 通过 + md5 核对部署一致。
- 2026-09-20：每本书工作区补「目标书无存档时清空文档」——用户要求切换到没有工作区存档的书时关闭所有打开的文档。实现：main.ts 新增 private closeOpenDocs()（workspace.iterateRootLeaves 收集主编辑区 type=markdown 叶子后逐个 leaf.detach()，侧栏停靠/浮动窗口不动，与工作区 getLayout/changeLayout 作用域一致）；loadBookWorkspace/applyStorySwitch 增加第二参 closeIfNoArchive（默认 false），无存档分支仅在 flag=true 时调用。传 true 的三个显式切书入口：cmdSwitchStory `/dir` 选择器、statusSwitchStory 写字台下拉框、cmdNewStory 新书激活（新书必无存档→旧书页签全清）；activeStory 隐式解析、pickWorkDir 自动加载、statusRunWriting 右键写作不传（避免命令运行时误清用户会话）。验证：npm run build tsc 零错误 + node --check 通过 + md5 核对部署一致。docs/使用说明.md、agents/workdir-behavior.md、code-structure.md 同步语义。
- 2026-09-20：workspaces/ 目录启动自检——用户要求代码中增加 workspace 文件夹检测、不存在就创建。实现：story_manager.ts 新增 public ensureVaultDir(dir)（adapter.exists → 缺失则 recursive mkdir，返回 exists|created）＋ private ensureParentDir(path)，writePluginFile 落盘前先自愈父目录；main.ts writeWorkspaceHeartbeat 在写 .heartbeat.txt 前先显式检测 workspaces/（console.info「已存在／原本缺失，已创建」），heartbeat 内容追加 dir_state 字段供磁盘侧核对。验证：npm run build tsc 零错误 + node --check 通过 + md5 核对部署一致。agents/workdir-behavior.md 同步 IO 语义。
- 2026-09-20：每本书独立 Obsidian 工作区整批（切书统一入口 applyStorySwitch 归档旧书/恢复目标书＋无存档时显式切书清空主编辑区文档页签＋layout-change 防抖 1500ms 自动存当前书＋rename/delete-story 迁移清理存档＋workspaces/ 目录启动自检与写前自愈＋IO 失败一次性带真实报错通知＋桌面端相对路径写入 getFullPath 绝对路径回退）打包发布 **v0.2.0**（Gitea tag v0.2.0 + GitHub 镜像无 v 前缀 tag 触发 CI；发布前移除 .heartbeat.txt 诊断机制，保留 console 留痕的启动目录检测）。
